# C 题高分完整方案（v4 冻结版）

> 服务于脑机接口与精神性疾病诊断的脑电图计算模型
> 本文件为**方案设计文档**（不含代码）。v4 在 `C题高分完整方案_v3.md` 基础上完成：**创新收口**（把创新收敛为单一联合框架并给出统一数学表达）、**学术防撞车**（对已有方法逐项认账并给出引用计划）、**竞赛主线压缩**（正文主线固定、图压缩至 9 张、支撑材料移入附录），并修正可靠度观测、低通道方法边界与反应时模型可辨识性。
> **冻结状态：已冻结；冻结日期：2026-09-23。** 本文件是后续编码、调试、实验与论文写作的**唯一设计基线**；v1、v2、v3 均仅作历史存档。
> **变更门槛：**只有复现实验发现事实错误、实现证明方案不可行，或用户明确批准升级版本时，才允许修改本文件。任何变更必须先记录到根目录 `version.md`，说明证据、影响与回退方案；禁止根据测试集结果静默改假设、分析窗、阈值、数据划分或准入标准。

---

## 相对 v3 的关键修订说明（六项，对应六项任务）

1. **新增"相关方法与学术防撞车矩阵"**（第 7 章）：逐项比较 xDAWN、去噪源分离、伪迹子空间重构及其低密度应用、状态空间事件相关电位提取、事件相关电位动态因果模型、漂移扩散/竞争证据累积、严格交叉验证下的脑电预处理。矩阵明确"已有方法解决什么、通道需求、是否处理长削顶、是否验证刺激保真、与本方案重合部分、本方案仅针对本题新增部分"。**不得把已有算法重新命名为原创方法。**
2. **创新点重新定义**（第 8 章）：不再声称状态空间模型、固定空间基、嵌套交叉验证、漂移扩散模型本身具有原创性。核心创新收敛为："**面向三电极、长平台削顶、设备滤波不可用的数据，建立事件语义解析—逐时刻观测可靠度—刺激特征保真约束—可辨识性裁决的一体化闭环**"，并给出四条统一数学表达（可靠度场、观测协方差调制与缺失化、带保真约束的内层选择、外层一次性评估）。
3. **增加强基线**：问题一至少比较普通带通加稳健平均、硬剔除、短段插补加长段降权、本方案可靠度加权观测模型四者（xDAWN、伪迹子空间重构仅作文献基线讨论，不强行在三通道上运行）；问题二必须比较训练折拟合的有限脉冲响应基线与 M1；问题三必须比较常数反应时模型、简单分布模型与竞争累积模型。
4. **压缩竞赛主线**（第 9 章）：正文主线固定为三句（事件语义解析与可靠度加权视觉响应提取；固定可观测空间模式上的输入—状态—观测模型及左右差异检验；行为反应时生成模型与条件准入的脑电弱耦合）；正文图压缩至最多 9 张；参数恢复、完整风险登记、扩展模型与失败结果移入附录；正文保留主要阴性结论。
5. **新增"创新主张允许与禁止清单"**（第 8 章末）：允许四类主张、禁止五类主张，逐条列出。
6. **新增正式参考文献计划**（附录 E）：为每个非原创组件指定应引用的原始或代表性论文并给出正文引用位置；**不虚构任何文献、作者、题目或结论**，全部条目须经文献管理器核对后方可写入论文。

**未新增任何模型**：可靠度加权观测模型是对 v3"质量标注—加权平均"的收敛重构（状态方程结构不变）；三级反应时基线是既有模型的最小实现。

---

## 相对 v1（旧版）的关键修订说明（沿自 v2/v3，保留）

### 一、删除（无法由 3 个额区电极支撑的过度主张）

| 旧版主张 | 删除理由 |
|---|---|
| 同时独立估计 V1 左右、额叶左右、海马等多个"真实源" | 3 个电极最多约束 3 个空间模式，多源反演存在不可辨识方向，属过度解读 |
| 把 Gabor 滤波器组、高斯差、左右镜像输入当作"数据支持的主要证据" | 这些是人为设定的编码层，不能反过来"证明"数据中的左右差异 |
| 用 Wilson–Cowan 多源神经群作为问题二主模型 | 参数多、可观测量少，拟合优度提升无法归因，答辩时无法自证 |
| 把"公共/差分分解"写成既定事实 | 该分解是否会损害刺激特征必须由外层验证裁决，属候选方法 |
| 用第 7 通道零延迟相关当作心电污染证据 | 零延迟相关既不能定位心拍时相，也不能排除容积传导滞后，只能作筛查量 |
| 把项目一/项目二反应时差当作严格的因果效应 | 任务与记录时段、疲劳、学习、记录质量完全混杂 |
| 声称可从单试次脑电可靠估计海马状态或决策斜率 | 单试次信噪比约 0.1，须先通过外层验证才可主张 |
| 20 张图（含纯概念图、无法验证的机制图） | 压缩为正文 9 张 + 附录图 |

### 二、降级（保留但改变地位）

| 内容 | 旧版地位 | v2/v3/v4 地位 |
|---|---|---|
| 神经群动力学（兴奋—抑制耦合、S 型传递） | 问题二主模型 | **理论说明与敏感性分析**：只用于解释参数含义与给出先验范围，不作为结论载体 |
| LGN、视觉皮层、海马 | 被反演的源区 | **生理解释标签与先验约束**：只解释"延迟""慢成分"的生理来源，不声称定位 |
| 刺激编码层（组合特征单元） | 左右差异的机制之一 | **受约束的外部输入构造**：只提供事件的形状与时刻，不参与左右差异的论证 |
| 左右差异的三种机制 | 并列竞争并被"判定" | 收敛为**同一模型内两个可辨识参数子集**（增益 vs 时序）的假设检验，不可区分时如实报告不可辨识 |
| 漂移扩散决策模型 | 问题三主体 | 问题三**优先解释反应时分布差异**；脑电耦合主张须通过外层验证方可写入 |

### 三、新增（v2/v3 起）

1. 可辨识性预算：先算"3 个电极能支撑几个自由度"，再决定模型规模。
2. 固定空间基（由电极几何定义，不由数据估计）：消除尺度与旋转不确定性；**其来源是经典正交源导联/双极导联，非原创（v4 起明确认账并引用）**。
3. 一个**真正可拟合的最简主模型**（线性三模式状态空间）+ 明确的逐级扩展与降级路径。
4. **嵌套连续试次块交叉验证**：内层选处理链与超参数，外层只报告最终指标。
5. **置换检验的严格分工**：受限替代事件检验（事件锁定）与块内受限标签置换（左右差异），任务差异另立为记录层面描述性比较。
6. 多重比较的显式处理：题面驱动并在编码前冻结的主分析 + 最大统计量族错误率控制 + 探索性分析标注。
7. 三条决策规则（去噪降级规则、模型降级规则、截断一致性规则）。
8. **可复现性与风险登记**：主张—证据—图表—失败判据—降级方案（18 条，v4 起移入附录 C，正文保留摘要与主要阴性结论）。

### 四、保留

- 第 2 章事件结构严格定义与第 9 通道语义论证（**核心资产，原样保留**）；
- 第 1 章数据质量审计（削顶分布、平台长度、坏试次比例、第 4–6 通道不可用性）；
- 全部**阴性结果**：100% 有反应且 100% 正确、一致性效应不存在、第 4–6 通道与原始信号相关性低、朴素流程峰值 t 值仅 1–2.5；
- 写作边界（不做诊断、不做人群推断、不宣称中央—顶区 P300、不把分类当主体、不隐藏阴性结果）。

---

## 0. 结论速览（v4 修订后）

1. **第 9 通道在项目一与项目二含义不同**，标签字符串已写明：项目一 `Action:L-1/R+1`（**完全没有 ±2**，±1 持续段的**终点就是反应时刻**），项目二 `TgtAct:L-2/R+2`（±1 段编码目标呈现侧，单点 ±2 编码点击侧，二者逐试次重合）。
2. **数据里 100% 试次有反应、100% 点击与目标同侧**：错误与漏答分支**无法用本数据验证**。
3. **项目二提示方向与目标侧无关**（一致率 47/100、48/100）：不存在设计好的"一致/冲突"因子，按一致性分组得到的反应时差在两人身上符号相反，是噪声。
4. 全数据最稳的行为现象是**任务间反应时差**（项目二比项目一长约 0.21–0.25 秒）。**该差异是记录层面的描述性跨记录差异**（每受试者内报告分布差、稳健效应量、分位数差与自助置信区间），任务与记录时段、疲劳、学习、记录质量完全混杂，**不作严格因果推断**。
5. **可辨识性预算决定模型规模**：3 个电极 → **最多 3 个可观测空间模式**（固定正交导联基，不由数据估计，其来源为经典重参考/源导联技术，非原创）。主模型是**线性三模式状态空间**；神经群、外侧膝状体核、海马只作为生理解释与先验约束。**参数可辨识性只能由留出预测、剖面似然、费舍尔信息与参数恢复模拟四项共同证明。**
6. **核心创新（唯一主张）**：面向三电极、长平台削顶、设备滤波不可用的数据，建立"**事件语义解析—逐时刻观测可靠度—刺激特征保真约束—可辨识性裁决**"的一体化闭环（第 8 章给出统一数学表达）。其中可靠度加权卡尔曼估计、状态空间模型、交叉验证本身**均非原创**。
7. **强基线**：问题一比较四基线（B1–B4，B4 为本方案可靠度加权观测模型）；问题二比较训练折拟合的有限脉冲响应基线；问题三比较常数反应时、简单分布与竞争累积三级模型。xDAWN、伪迹子空间重构仅作文献基线讨论。
8. 问题三把反应前 **100 / 300 / 500 毫秒**三档截断作为**并列敏感性分析**；**仅当效应方向一致且效应量及其置信区间无实质性冲突**时，才作"与认知相关"的谨慎解释（显著性差异仅作功效信息）。

---

## 1. 数据事实（保留 v3 审计，全部标注复现状态）

> **复现声明**：本章所有具体数值（采样点计数、削顶计数、试次编号、p 值、反应时）均来自一次性只读探查，**待正式固定随机种子的脚本复现**。凡与复现结果不一致者，以复现脚本输出为准，并更新本文档与图表。复现脚本必须固定随机种子、固定阈值来源（`configs/cti.yaml`），且不得读取本系列文档中的数字作为输入。

### 1.1 四组数据总表

| 文件 | 采样点 | 时长 | 提示事件（左/右） | 第 9 通道结构 | 提示结束→目标呈现 |
|---|---:|---:|---:|---|---|
| A 项目一（`VisualCogA_Task-1.mat`） | 223845 | 874.40 秒 | 100（54/46） | 100 段 ±1，**无 ±2** | 515/516 点 |
| A 项目二（`VisualCogA_Task-2.mat`） | 230157 | 899.05 秒 | 100（57/43） | 100 段 ±1（48/52）+ 100 个**单点** ±2 | 515/516 点 |
| B 项目一（`VisualCogB_Task-1.mat`） | 223005 | 871.11 秒 | 100（48/52） | 100 段 ±1（48/52），**无 ±2** | 515/516 点 |
| B 项目二（`VisualCogB_Task-2.mat`） | 228330 | 891.91 秒 | 100（50/50） | 100 段 ±1（46/54）+ 100 个单点 ±2 | 515/516 点 |

其他已核验事实（待复现）：采样率 256 赫兹；第 10 通道时间戳单位为**秒**、步长恒为 1/256、无丢样；提示时长恒为 52 或 53 个采样点；提示结束到目标呈现绝大多数为 515 或 516 个采样点，偏差不超过 4 个采样点，唯一明显例外是 A 项目一第 1 试次（544 个采样点，反应时亦为全数据极值，判为热身试次）；记录开头约 20 秒引导段、结尾约 23 秒尾段，**中间无中断**；试次间隔中位数 8.34–8.63 秒。

### 1.2 行为结构（待复现；统计口径见第 6 章）

| 文件 | 反应时中位数 | 左目标 | 右目标 | 左−右 |
|---|---:|---:|---:|---:|
| A 项目一 | 约 1.14 秒 | 约 1.16 | 约 1.13 | 约 +0.03 秒 |
| A 项目二 | 约 1.39 秒 | 约 1.44 | 约 1.34 | 约 +0.09 秒 |
| B 项目一 | 约 1.11 秒 | 约 1.12 | 约 1.10 | 约 +0.02 秒 |
| B 项目二 | 约 1.32 秒 | 约 1.37 | 约 1.27 | 约 +0.10 秒 |

**解释纪律**：项目一与项目二在同一受试者内先后记录，任务条件与**记录时段、疲劳、学习、注意力波动、电极与放大器的记录质量**完全混杂（例如 B 项目二 F4 的削顶样本数远高于 B 项目一）。因此：

- 任务差异只作**记录层面描述性比较**：每受试者内报告两记录的反应时分布差（中位数差、稳健效应量）、分位数差（25/50/75 百分位）与试次级自助置信区间（2000 次）；
- **任务标签不可在试次级置换**，不报告任务差异的置换 p 值；
- **不写**"位置不确定性导致反应时增加 0.22 秒"这类因果句；
- 若复现后两人方向不一致，则该差异降级为"描述性观察，不构成效应"。

**阴性结果（保留）**：项目二点击侧与目标侧一致率 100/100（无错误、无漏答）；提示方向与目标侧一致性效应在两人身上符号相反。

### 1.3 信号质量（待复现）

| 文件 | Fz 削顶（负/正） | F3 | F4 | 提示后窗内存活试次 |
|---|---:|---:|---:|---:|
| A 项目一 | **0 / 0** | 0 / 4573 | 0 / 6711 | 67 / 100 |
| A 项目二 | **0 / 0** | 452 / 2825 | 410 / 963 | 43 / 100 |
| B 项目一 | 610 / 3135 | 0 / 2246 | 0 / 2921 | 51 / 100 |
| B 项目二 | 113 / 237 | 484 / 2809 | 678 / 5158 | 40 / 100 |

- 削顶值精确等于 ±1000.0，且几乎全部为**正向平台**；最长恒定值段即 +1000 平台，约 478 与 523 个采样点（约 1.87 与 2.04 秒）。
- 原始标准差约 53–325；第 4–6 通道标准差约 7–8。
- 第 4–6 通道与 0.3–35 赫兹零相位滤波后的原始通道相关系数约 0.09–0.71，低频被削平且仍含大瞬态：**既不是干净脑电，也不能当噪声参考**（保留结论）。
- 朴素流程（0.3–35 赫兹零相位 + 基线校正）在 250–550 毫秒窗内峰值 t 值约 1.0–2.5，峰潜伏期多集中在约 320–480 毫秒。
- 硬剔除（饱和或峰峰值超阈）后只剩约 40–67 个试次，说明硬剔除可能显著损失统计效能，构成比较可靠度加权方案的动机；**最终采用 B2、B3 还是 B4 仍由预先规定的外层比较裁决**，不得先验宣布 B4 胜出。
- 心电通道：第 7 通道的**零延迟相关最高约 0.39（B 项目一）**，但该量**只作筛查**，不作为污染证据；严格验证见 3.3。
- 工频：50 赫兹峰/中位数比约 1.1–2.0，非主要矛盾。

### 1.4 陷阱清单（保留，写作时必须引用）

1. **第 9 通道语义随任务改变**；用"±2 即点击"的单一解析器读项目一，会得到 0 个点击事件。
2. 项目一没有 ±2，**±1 段终点即反应时刻**。
3. 项目一中"目标侧"与"反应侧"同号、与提示 98–100% 一致，**不可分辨**；B 项目一有 2 个互换成对的不一致试次（旧版记录为第 49、87 试次，**待复现**），必须单独标记并做双标号敏感性分析。
4. 数据 **100% 有反应、100% 正确**，与题面相反：错误与漏答分支无法验证。
5. 项目二提示方向与目标侧无关；"一致/不一致"不是设计因子。
6. 削顶是**单侧正向平台**（最长近 2 秒），不是零星坏点。
7. 削顶分布**四个文件完全不同**，不能共用阈值与结论（A 项目一 Fz 完全干净，B 项目一 Fz 大量削顶）。
8. 恒定 +1000 平台易被误判为"电极脱落零漂"。
9. **3 个通道不能做可靠独立成分分析**。
10. 第 4–6 通道"看起来干净"是重滤波结果。
11. 时间戳单位是秒；"无丢样"不等于"数据可用"。
12. 提示消失后约 2.0 秒空屏是**工作记忆维持期**，不是空白基线。
13. 只有 2 名受试者：**任何人群级、疾病诊断表述都不成立**。
14. 未采集中央—顶区，正向响应只能称"候选正向事件相关响应窗"。
15. 反应时约 0.8–2.0 秒，点击前数秒已含运动准备成分 → 见第 5 章三档截断。
16. A 项目一第 1 试次为热身异常。

---

## 2. 事件结构的严格定义（保留，本次修订的核心资产）

### 2.1 统一时间基准与符号

以第 8 通道（视觉提示）上升沿为试次零点 $t=0$：

- $t_{\text{cue,on}}(k)$：第 8 通道由 0 跳变到 $\pm1$ 的采样点；
- $t_{\text{cue,off}}(k)=t_{\text{cue,on}}(k)+52\ \text{或}\ 53$；
- $t_{\text{tgt,on}}(k)$：第 9 通道由 0 跳变到 $\pm1$ 的采样点，通常为 $t_{\text{cue,off}}(k)+515$；
- $t_{\text{resp}}(k)$：第 9 通道 $\pm1$ 持续段**结束**的采样点；项目二中等于 $\pm2$ 所在采样点；
- $\mathrm{RT}(k)=t_{\text{resp}}(k)-t_{\text{tgt,on}}(k)$。

侧别标签：$\ell_{\text{cue}}$（第 8 通道段值符号）、$\ell_{\text{tgt}}$（第 9 通道 ±1 段值符号）、$\ell_{\text{resp}}$（±2 值符号，**仅项目二可观测**；项目一按句法取 $\ell_{\text{resp}}\equiv\ell_{\text{tgt}}$ 并声明不可分辨）。

分析窗口：

| 窗口 | 区段 | 用途 |
|---|---|---|
| $W_{\text{cue}}$ | $[-0.5,+1.0]$ 秒 | 问题一主分析 |
| $W_{\text{tgt}}$ | 以目标为零点的 $[-0.5,+1.0]$ 秒 | 问题三对比分析 |
| $W_{\text{base}}$ | $[-0.5,-0.2]$ 秒 | 逐试次稳健基线 |
| $W_{\text{cog}}^{(c)}$ | $[t_{\text{cue,on}}-0.5,\ t_{\text{resp}}-c]$ 秒，$c\in\{100,300,500\}$ 毫秒 | 问题三**三档并列**分析 |

### 2.2 第 9 通道在项目一与项目二的不同含义

| 对比项 | 项目一 | 项目二 |
|---|---|---|
| 数据自带标签 | `Action:L-1/R+1` | `TgtAct:L-2/R+2` |
| ±1 持续段 | 起点 = 目标呈现，终点 = 反应 | 起点 = 目标呈现，终点 = 反应 |
| 段值含义 | 目标侧（= 反应侧，同号，不可分辨） | **目标呈现侧**（与提示方向无关） |
| 单点 ±2 | **不存在** | 存在 100 个，恰为反应时刻 |
| 反应时刻获取 | 用 ±1 段终点 | 用 ±2 采样点（与段终点逐试次相同） |
| 左右对比归因 | 提示侧 = 目标侧 = 反应侧（复合，不可分离） | 目标侧与提示解耦，唯一可做位置不确定性分析的通道 |

**严格表述**：

> 项目一的第 9 通道把"目标出现在哪一侧"与"受试者点击了哪一侧"压缩进同一个 ±1 持续段，其终点即反应时刻，通道本身无法区分感知与反应两种来源；项目二把二者拆开，±1 段编码目标呈现侧，单点 ±2 编码点击侧。由于项目二中 ±2 的采样点索引与 ±1 段终点在全部 100 个试次上重合，可确认 ±1 段的结束不是"目标消失"而是"反应发生"。

### 2.3 认知阶段划分（供问题三使用）

| 阶段 | 时间区间 | 主导过程 | 数据依据 |
|---|---|---|---|
| 阶段一 视觉直接响应 | $[t_{\text{cue,on}},+0.4]$ 秒 | 快速感觉驱动 | 提示锁定平均在约 300–500 毫秒出现正向峰（待复现） |
| 阶段一′ 维持 | $[+0.4,\ t_{\text{tgt,on}}]$ 秒（约 2.0 秒空屏） | 无外源视觉输入 | 提示已消失、目标未出现 |
| 阶段二 目标加工与匹配 | $[t_{\text{tgt,on}},\ t_{\text{resp}}-c]$ 秒 | 目标识别与模板比对 | 目标锁定与提示锁定曲线之差 |
| 动作（排除） | 最后 $c$ 毫秒 | 运动准备与执行 | 点击前数百毫秒已有负向成分（待复现） |

### 2.4 事件清单产物与单元测试断言

产物：`trials.csv`，字段为
`file, subject, task, trial_index, t_cue_on, cue_dur, t_tgt_on, cue2tgt_gap, t_resp, RT, side_cue, side_tgt, side_resp, congruent, bad_frac_Fz, bad_frac_F3, bad_frac_F4, n_sat, warmup_flag, side_conflict_flag`

**断言（复现脚本必须逐条通过）**：

1. 每文件提示事件恰 100 个；
2. 提示时长 $\in\{52,53\}$；
3. 提示结束→目标间隔 $\in[515,519]$，且仅 A 项目一第 1 试次为 544；
4. 项目二 ±2 个数 = 100 且索引等于前一 ±1 段终点；
5. 项目一 ±2 个数 = 0；
6. 项目二 $\mathrm{sign}(\pm2)$ 与 $\ell_{\text{tgt}}$ 一致率 100%；
7. 项目二 $\ell_{\text{cue}}$ 与 $\ell_{\text{tgt}}$ 一致率落在 45%–52%；
8. 无试次缺少反应段；
9. 时间戳差分唯一值为 $1/256$；
10. 正式流程中第 4–6 通道出现次数为 0（仅"为何不可用"的对比实验允许出现）。

---

## 3. 问题一：事件语义解析与可靠度加权视觉响应提取（v4 收敛）

### 3.1 选择与报告分离：嵌套连续试次块交叉验证

处理链与阈值的选择不得与最终结论共用同一批数据。

- **外层**：每个记录按**连续试次块**（每块 20 试次）分 5 折。外层只做三件事：报告最终指标（事件相关曲线与置信带、单试次分类 AUC、条件差异及其检验、模型性能）。外层不得再回头调整任何参数。
- **内层**：在 4 个训练折内部再做 4 折，用于选择：高通截止（0.1/0.3/0.5 赫兹）、提取方案（3.2 的 B1–B4）、可靠度阈值、拟合窗口。
- **一切数据相关量只能在训练折内估计**：坏点阈值、伪迹模板（眨眼、心拍）、归一化系数、白化矩阵、分类器超参数、模型参数。
- **同时报告选择稳定性**：每个选项在内层被选中的折数比例。若某选项在 5 折中被选中次数 $\le2$，则该选项不具备稳定性，不得作为正式方案。
- **特殊性说明**：事件相关曲线本身不是"预测"，因此对外层试次计算曲线仍属合法报告；但**条件差异的显著性必须在外层留出试次上计算并检验**，不得用全部试次算差异再报 p 值。

### 3.2 处理链：可靠度场 + 四基线 + 决策规则

**第一层 质量标注（只标注，不修改幅值）**：饱和（$\lvert x\rvert=1000$ 精确判据）、恒定平台（连续相同值 $\ge3$ 点）、瞬态（$\lvert\Delta x\rvert>8\times$ 差分中位绝对偏差）、跨通道不一致（以三通道加权中位数为稳健公共模式，残差超 $6\times$ 残差中位绝对偏差判为不可信）。

**第二层 可靠度场 $w_k(t)$（v4 收敛，第 8 章统一数学的第 1 条）**：对通道 $k\in\{\mathrm{Fz},\mathrm{F3},\mathrm{F4}\}$、逐时刻 $t$：

$$w_k(t)=\big[h^{\text{sat}}_k(t)\cdot h^{\text{plat}}_k(t)\big]\cdot s^{\text{tr}}_k(t)\cdot s^{\text{cross}}_k(t)\ \in[0,1]$$

- 硬因子（取 0/1）：$h^{\text{sat}}_k(t)=0$ 当 $\lvert x_k(t)\rvert=1000$；$h^{\text{plat}}_k(t)=0$ 当样本处于 $\ge3$ 点恒定平台；否则为 1。
- 软因子（连续衰减）：$s^{\text{tr}}_k(t)=1\big/\big(1+(\lvert\Delta x_k(t)\rvert/\tau_{\text{tr}})^2\big)$，$\tau_{\text{tr}}=8\times$ 差分中位绝对偏差；$s^{\text{cross}}_k(t)=1\big/\big(1+(r_k(t)/\tau_{\text{cross}})^2\big)$，$r_k=x_k-m$（$m$ 为加权中位数），$\tau_{\text{cross}}=6\times$ 残差中位绝对偏差。阈值 $\tau$ 由内层在训练折内选择。
- 每试次可靠度 $w_k^{\text{trial}}=\mathrm{mean}_t\,w_k(t)$ 派生试次权重。

**第三层 提取与去噪候选（四基线并列，内层裁决）**：

| 编号 | 方案 | 关键要素 | 地位 |
|---|---|---|---|
| B1 | 普通带通 + 稳健平均 | 0.3–35 赫兹零相位 + 20% 截尾平均，无坏点处理 | 保守基线 |
| B2 | 硬剔除 + 平均 | 饱和或峰峰值超阈整试次剔除后平均 | 对照（代价大：只余 40–67 试次） |
| B3 | 短段插补 + 长段降权 + 稳健平均 | 仅 $\le0.1$ 秒短段插补；长平台降权不修复 | 对照 |
| B4 | **本方案：可靠度加权观测模型** | 潜在状态用固定正交导联基表示，但卡尔曼观测更新保留在 Fz/F3/F4 **传感器空间**；对 $0<w_k(t)\le1$ 令观测方差随可靠度降低而增大，对 $w_k(t)=0$ 的传感器删除该时刻观测行，不插值；是否使用三模式状态表示由内层决定 | 正式候选 |

- **xDAWN、伪迹子空间重构只作文献边界讨论**（见第 7 章矩阵与附录 D）：xDAWN 在 3 通道下仍可估计空间滤波，但空间自由度低、协方差估计易受长平台削顶影响，性能优势必须实证；ASR 已有 8 通道低密度有效性证据，但该结果不能直接外推至 3 通道和近 2 秒平台削顶。二者不纳入主处理链，且**不得表述为“算法不适用”**；如工期允许，可把正则化 xDAWN 作为补充敏感性基线。
- **可靠度加权本身是标准技术**（加权最小二乘/卡尔曼滤波，见附录 E），非原创；针对本题的联合设计见第 8 章。

**决策规则 D1（去噪降级规则）**

> 若 B4 在外层留出块上的保真指标（单试次左右可分性 AUC、分半可靠性）**低于 B1–B3 中的最优者**，或事件锁定统计量的提升幅度在 5 折中有 2 折以上无法复现，则**退回该最优保守基线**，并在论文中如实报告"B4 未能通过外层保真检验，正式方案为保守基线"。若与最优基线相当，选更简者。

**第四层 保真门控（候选，可被 D1 一并否决）**：对候选噪声成分（眨眼模板、心拍模板、漂移、运动瞬态）：以事件锁定统计量对**受限替代事件检验**（6.2）的零分布做判定，不显著才可抑制；显著锁定或显著区分条件者只做软阈值收缩**不清零**。**该思想的出处是去噪源分离（见第 7 章），非原创，必须引用。**

**第五层 时域滤波**：0.3 赫兹零相位高通（内层可选 0.1/0.3/0.5）+ 35 赫兹低通；50 赫兹零相位带阻（工频非主要矛盾，保留以显示规范）。

**第六层 曲线与参数化拟合**：逐时间点可靠度加权稳健均值 + 自助法（2000 次）置信带；参数化拟合 $y(t)=a_1G(t;\mu_1,\sigma_1)+a_2G(t;\mu_2,\sigma_2)+bt+c$；单试次响应强度用模板最小二乘投影估计。

**表述纪律**：250–500 毫秒窗称"候选正向事件相关响应窗"，报告实测潜伏期区间，说明未采集中央—顶区，不等同于经典 P300。

### 3.3 心电污染的严格验证（修订）

**旧版做法不可接受**：直接用第 7 通道与脑电的零延迟相关作为污染证据。

**两步验证**：

1. **心拍对齐平均**：第 7 通道带通（约 5–25 赫兹）后峰值检测（不应期约 250 毫秒）得心拍标记；以心拍为零点对三个脑电通道分段平均；用心拍时刻随机移位构造零分布检验。通过才认定存在心电污染。
2. **滞后回归**：心电模板（心拍对齐平均波形）对脑电的滞后回归（±200 毫秒），报告偏回归系数、滞后峰值与解释方差。

**决策规则 D2**：两步均显著才在观测方程中引入心电回归项；否则不引入并写明"零延迟相关仅作筛查，未通过心拍对齐检验"。

### 3.4 问题一的检验与多重比较落点

检验与多重比较的**统一定义见第 6 章**。问题一的具体落点：

| 对象 | 检验方法（定义处） | 说明 |
|---|---|---|
| 提示锁定响应是否存在 | 受限替代事件检验（6.2） | 主分析 = **题面驱动并在编码前冻结**：题面给定 250–500 毫秒 P300 范围，结合已采集的额中电极 Fz，检验正向峰；唯一，不校正；不得宣称独立预注册 |
| 左右差异 | 块内受限标签置换（6.3） | 保持每时间块左右计数不变；同一记录内 |
| 候选成分保真门控 | 受限替代事件检验（6.2） | 置换零分布 95% 分位为阈值 |
| 三通道 × 窗内时间点 | 最大统计量法（6.4） | 族错误率零分布由相应置换生成 |
| 任务差异 | 记录层面描述性比较（6.5） | **不置换任务标签** |

### 3.5 问题一的验证指标（修订）

| 类别 | 指标 | 判定 |
|---|---|---|
| 噪声 | 基线窗残余标准差、通道间残差标准差 | 下降 |
| 保真 | **外层留出块**单试次左右方向 AUC | 不下降（B4 对 B1–B3 的裁决依据） |
| 保真 | **外层留出块**分半可靠性 | 不下降 |
| 保真 | 滤波前后 0–30 赫兹互谱相干与群延迟 | 群延迟 ≈ 0 |
| 证据 | 主分析的最大统计量与置换 p 值 | 报告实际值 |
| 稳健 | 内层选择稳定性（各选项被选中折数） | $\ge3/5$ |
| 可复现 | 四组数据分别报告，方向一致性 | 逐文件，不合并 |

**正文主表（表 2）**：B1–B4 四行 ×（内层选中折数、外层 AUC、外层分半可靠性、基线残余标准差、主分析最大统计量与校正 p）。

---

## 4. 问题二：固定可观测空间模式上的输入—状态—观测模型（保留 v3 主体）

### 4.1 可辨识性预算：先算自由度，再定模型规模

- 观测：每时刻 3 个电极值 → 瞬时线性混合模型最多约束 **3 个空间模式**。
- 若允许任意混合矩阵 $A$，则 $y=Ax$ 中 $A$ 与 $x$ 存在任意可逆变换的不确定性（尺度、旋转、置换）。**没有额外约束时，"多个源"在数学上不可辨识。**
- 两条硬约束：
  1. **空间基由电极几何固定，不由数据估计**（见 4.2），从而消除尺度与旋转不确定性；
  2. **模型规模上限为 3 个可观测空间模式**，**不声称同时独立估计视觉左右、额叶左右、海马等多个真实源**。
- **明确删除的错误论证**：不采用"参数数远小于采样点数，因此可辨识"的说法。**参数可辨识性只能由四项共同证明：留出预测、剖面似然、费舍尔信息、参数恢复模拟**（见 4.7）。四项中任一失败，对应参数即不得解释。

**生理解释的地位（关键表述，答辩必答）**：

> 外侧膝状体核、视觉皮层、海马在本文中**仅作为参数的生理解释与先验约束**，**不作为由 F3、Fz、F4 直接定位或独立反演的对象**。本文不提供任何源定位结论。

### 4.2 固定空间基（由电极几何定义；来源为经典导联技术，非原创）

通道顺序固定为 **Fz、F3、F4**。取三个正交固定模式：

$$\mathbf{m}_1=\frac{1}{\sqrt3}(1,1,1)^{\mathsf T},\qquad
\mathbf{m}_2=\frac{1}{\sqrt2}(0,1,-1)^{\mathsf T},\qquad
\mathbf{m}_3=\frac{1}{\sqrt6}(2,-1,-1)^{\mathsf T}$$

- $\mathbf{m}_1$：**全导联共模**；
- $\mathbf{m}_2$：**左右差模**（$(F3-F4)/\sqrt2$；Fz 权重为零），侧化信息的唯一载体；
- $\mathbf{m}_3$：**额中对比模**（$(2\,Fz-F3-F4)/\sqrt6$，Fz 与双侧平均的对比）。

三者两两正交且归一，模式时间序列可直接由投影得到：$s_j(t)=\mathbf{m}_j^{\mathsf T}\mathbf{y}(t)$。

**学术认账（v4 新增）**：该基是**平均参考 + 双极导联 + 拉普拉斯式组合的标准化正交表达**，其思想源于经典正交源导联（Hjorth 1975）与脑电通信空间滤波（McFarland 1997），**不是新算法**（见第 7、8 章与附录 E）。优点是不需要估计混合矩阵；代价是模式不等于解剖源。

### 4.3 最简主模型 M1：线性三模式状态空间（含可靠度观测）

对每个模式 $j\in\{1,2,3\}$，设兴奋性状态 $x_{E,j}$ 与慢抑制/适应状态 $x_{I,j}$：

$$\tau_{E,j}\,\dot x_{E,j}(t)=-x_{E,j}(t)-c_j\,x_{I,j}(t)+\sum_{e\in\{\text{cue},\text{tgt}\}} g_{j,e}\,u_e\!\big(t-d_{j,e}\big)+w_{E,j}(t)$$

$$\tau_{I,j}\,\dot x_{I,j}(t)=-x_{I,j}(t)+x_{E,j}(t)+w_{I,j}(t)$$

观测更新必须保留在**传感器空间**。令 $\mathbf z(t)=[x_{E,1}(t),x_{E,2}(t),x_{E,3}(t)]^{\mathsf T}$，$\mathbf M=[\mathbf m_1,\mathbf m_2,\mathbf m_3]$，可用传感器集合为 $A(t)=\{k:w_k(t)>0\}$，则：

$$\mathbf y_{A(t)}(t)=\mathbf M_{A(t),:}\,\mathbf z(t)+\mathbf a_{A(t)}(t)+\boldsymbol\varepsilon_{A(t)}(t),$$

$$\mathbf R_{A(t)}(t)=\operatorname{diag}_{k\in A(t)}\!\left(\frac{\sigma_{0,k}^{2}}{\max\big(w_k(t),10^{-3}\big)}\right),\qquad 0<w_k(t)\le1.$$

当 $w_k(t)=0$ 时，直接从 $\mathbf y(t)$、$\mathbf M$ 与 $\mathbf R(t)$ 中删除该传感器对应行；若 $A(t)=\varnothing$，该时刻仅执行状态预测、不执行观测更新，**不强行插值**（标准缺失数据处理，Shumway & Stoffer 1982）。$10^{-3}$ 只限制正可靠度的数值条件，**绝不能把零可靠度替换成有限方差**。若为展示而投影到模式空间，则 $\mathbf R_m(t)=\mathbf M^{\mathsf T}\mathbf R_y(t)\mathbf M$ 通常为非对角阵，不能把三个模式误当作独立标量噪声。

其余符号：$u_{\text{cue}}$、$u_{\text{tgt}}$ 为事件输入核（时刻由第 2 章事件层给出）；$\mathbf a(t)$ 为传感器空间伪迹项（漂移随机游走 + 眨眼模板 + 心电模板，心电项仅在通过 3.3 检验时引入）；$\boldsymbol\varepsilon(t)$ 为可预白化的一阶自回归有色观测噪声。

**参数**：每模式 $\{\tau_{E,j},\tau_{I,j},c_j,g_{j,\text{cue}},g_{j,\text{tgt}},d_{j,\text{cue}},d_{j,\text{tgt}}\}$，共 21 个；"延迟跨模式共享"降为约 19 个。参数规模是否可接受**不由数据量论证**，而由 4.7 的四项证明裁决。

**拟合方法**：离散化后写成标准线性状态空间，用预测误差法或子空间辨识做初值，再以卡尔曼递推似然做极大似然精修；内层选复杂度（是否共享延迟、是否保留第 3 模式、是否引入非线性）；外层报告留出块预测误差，**训练折与测试折划分与所有基线模型完全相同**。

**可辨识性（四项证明，缺一不可）**：留出预测（4.4）；剖面似然；费舍尔信息；参数恢复模拟（4.7）。**可靠度加权卡尔曼估计与状态空间模型均为标准技术（Kalman 1960；Shumway & Stoffer 1982），非原创。**

### 4.4 基线 M0 与逐级扩展、降级路径

**强基线 M0（无状态动力学的预测基线，必须与 M1 同折比较）**

- **不得**使用包含测试试次的经验平均曲线作为预测基线（泄漏）。
- M0 采用与 M1 **相同的事件输入**、**相同的训练折与测试折**，但响应用正则化有限脉冲响应刻画：

$$\hat s_j(t)=\sum_{e\in\{\text{cue},\text{tgt}\}}\sum_{k=0}^{K-1}h_{j,e}[k]\,u_e(t-k)+b_j$$

- 核由**训练折**上的岭回归估计，抽头数 $K$ 由内层在训练折内选择，使 M0 的有效自由度与 M1 可比；测试折上计算预测误差。
- 替代实现 M0′：条件平均预测器——用**训练折内**同条件试次的逐时间点平均作为该条件留出试次的预测。测试试次不得进入任何平均。
- 所有模型比较（M0–E5）采用**相同训练折、相同测试折**，并报告各模型参数数/有效自由度与 AIC。**问题二必须报告"M1 对训练折拟合 M0"的留出预测误差对比（图 6）。**

| 级别 | 模型 | 加入内容 | 准入条件 | 不达标时 |
|---|---|---|---|---|
| M0 | 训练折拟合的正则化有限脉冲响应/条件平均（预测基线） | 无状态动力学 | 强基线对照 | — |
| **M1** | **线性三模式状态空间（主模型）** | 提示/目标两路输入、E–I 二阶动力学、可靠度观测 | 外层留出块预测误差优于 M0 | 退回 M0 并如实报告"模型未优于基线" |
| E1 | 简化 M1（延迟共享、参数减少） | 更少参数 | 与 M1 预测相当但参数更稳定 | 用 M1 |
| E2 | 两模式 M1（去掉 $\mathbf m_3$） | 降维 | $\mathbf m_3$ 的能量不显著高于噪声 | 用三模式 |
| E3 | 加输入增益的**条件依赖**（左/右提示不同增益或不同延迟） | 左右差异假设检验 | 外层预测改善且参数区间不含零点 | 报告"未检出条件差异" |
| E4 | 加模式间耦合（$3\times3$ 耦合矩阵的可辨识元素） | 交叉模式作用 | 外层预测改善 | 保持无耦合 |
| E5 | 加饱和非线性（S 型传递） | 弱非线性 | 外层预测改善 | 保持线性 |

**降级为理论说明的部分（不用于主张）**：完整的 Wilson–Cowan 兴奋—抑制神经群、外侧膝状体核的空间滤波（高斯差）、左右半野镜像结构、海马多区域耦合——只在"生理解释"小节出现，作敏感性分析，**不作为结论的证据**（扩展模型细节移入附录 D）。

### 4.5 刺激编码层的正确地位（保留）

**定位**：

> 刺激编码层唯一的作用是**构造一个受约束的外部输入**：把"左三角/右三角"转换为两个具有明确时刻与持续时间的驱动事件 $u_e(t)$。编码层给出的是**输入的时间结构**，不是**脑内的空间分布**。

三条纪律：

1. 输入只保留**事件核**：峰值时刻与宽度由刺激物理时间确定，不做图像级滤波；
2. 左右两条件在模型中的差别**只允许出现在参数上**（增益、延迟、时间常数），**不允许通过给左右设置不同的输入波形来"造出"差异**；
3. "形状敏感性"的理论讨论放附录，并声明是**理论讨论，不是数据证据**。

**左右差异只能通过以下三条观测证据检验**（均由三通道数据直接计算）：

| 观测量 | 计算方式 | 对应假设 |
|---|---|---|
| 幅度 | 差模 $\mathbf m_2$（即 $(F3-F4)/\sqrt2$）分量在响应窗内的峰值 | 增益不对称 |
| 潜伏期 | 差模分量的峰潜伏期与上升沿半宽 | 时序不对称 |
| 侧化模式 | F3 与 F4 的响应在左右条件间的**极性/相对大小反转** | 空间模式差异（由 $\mathbf m_2$ 的符号与幅度刻画） |

### 4.6 左右差异的可检验假设（保留）

| 假设 | 参数表述 | 观测特征 | 检验 |
|---|---|---|---|
| H1 增益不对称 | $g_{2,\text{cue}}^{(L)}\ne g_{2,\text{cue}}^{(R)}$ | 差模幅度差异，潜伏期不变 | 参数区间是否含零；外层预测改善 |
| H2 时序不对称 | $d_{2,\text{cue}}^{(L)}\ne d_{2,\text{cue}}^{(R)}$ 或 $\tau_{E,2}$ 条件依赖 | 潜伏期或上升沿差异 | 剖面似然（延迟与时间常数须分别报告） |
| H3 交叉模式作用 | 条件依赖的耦合元素 | 差模与共模的相对关系变化 | 仅当 E4 通过外层预测检验时 |

**不可区分时的强制表述**：若 H1 与 H2 的外层预测误差相近且参数区间均不含零，则**必须报告"本数据无法区分增益不对称与时序不对称"**，并只呈现描述性差异，不得挑选一个写成机制结论。

### 4.7 参数识别、可辨识性诊断与参数恢复模拟（四项证明）

1. **留出预测**：外层留出块的预测误差（与训练折拟合的 M0 同折比较）；
2. **剖面似然**：逐参数剖面似然曲线有界；"延迟—时间常数"退化用剖面似然 + 延迟共享 + 敏感性三重处理；尺度与符号由固定基与"共模增益为正"约定；
3. **费舍尔信息**：特征值谱与条件数，输出"有界参数"与"仅组合量可辨识"清单；
4. **参数恢复模拟（R15）**：用拟合参数为已知真值生成模拟试次（事件结构取自 `trials.csv`，噪声水平按拟合残差设定，随机种子写入配置），以**完全相同**的拟合流程重拟合；对每个参数报告恢复偏差、真值与估计的秩相关、置信区间覆盖率；判定阈值（写入配置）：秩相关 < 0.5、或覆盖率 < 60%、或 > 99% 判为恢复失败；**恢复失败的增益、延迟、时间常数不得在真实数据中解释**，在表 3 标"不可解释"并按先验值固定。

**诊断材料移入附录 D**（正文只报告结论与"可解释参数清单"）。**秩限制说明**：模式数 $\le3$；任何超过 3 个自由空间维度的模型都必须声明存在不可辨识方向。

### 4.8 特征表示与可分性（保留）

单试次特征向量（全部由可观测模式定义）：

$$\mathbf{f}=\big[\hat A_1,\hat A_2,\hat A_3,\ \mathrm{LI},\ \lambda_2,\ \mathrm{PLV}_2,\ \hat d_2\big]$$

其中 $\hat A_j$ 为第 $j$ 模式在响应窗内的稳健幅度，$\mathrm{LI}=\hat A_2/(\lvert\hat A_1\rvert+10^{-9})$ 为相对侧化指数，$\lambda_2$ 为差模峰潜伏期，$\mathrm{PLV}_2$ 为差模在 4–8 赫兹的相位一致性，$\hat d_2$ 为差模等效延迟估计。

**分类检验**：线性判别或逻辑回归；外层连续试次块 5 折交叉验证；标签打乱 1000 次零分布；对照组为原始波形点值与功率谱特征。合理预期 AUC 0.6–0.75。

### 4.9 问题二的验证、消融、统计与图表（保留，含强基线）

| 指标 | 定义 | 通过条件 | 失败后的降级 |
|---|---|---|---|
| 外层预测误差 | 留出块上模型预测与实测模式的均方误差，对比**训练折拟合的 M0**（同折） | 优于 M0 | 退回 M0，只报告描述性拟合 |
| 模型比较 | M0–E5 的交叉验证对数似然与 AIC/BIC（同折、自由度匹配） | 明确选优或明确报告不可区分 | 选最简者 |
| 参数稳定性 | 各折参数变异系数、自助法区间 | 区间不过宽 | 固定参数并报告组合量 |
| 生理一致性 | 延迟为正、时间常数落在先验范围 | 全部通过 | 报告越界参数并说明 |
| 可分性 | 外层 AUC 与置换零分布 | 显著优于原始波形特征 | 报告"模型特征未优于原始特征" |
| 可辨识性 | 四项证明 | 明确清单 | 失败参数标"不可解释"并固定 |

**统计**：左右条件差异用**块内受限标签置换**（6.3）；响应存在性用**受限替代事件检验**（6.2）；参数区间用自助法；模型比较用嵌套似然比或交叉验证。

---

## 5. 问题三：行为反应时生成模型与条件准入的脑电弱耦合（保留 v3 主体，加三级基线）

### 5.1 主张优先级的根本调整（保留）

| 优先级 | 主张 | 证据类型 | 是否可写入结论 |
|---|---|---|---|
| 一级 | 项目二与项目一反应时**分布**的描述性差异（记录层面） | 行为数据 | 可写（受 1.2 节混杂限制，仅描述性） |
| 二级 | 反应时分布可由竞争累积模型定量描述 | 行为数据 + 留出拟合指标 | 可写（描述性生成模型） |
| 三级 | 脑电模式轨迹与反应时的**单试次相关** | 必须通过外层交叉验证 | 仅在通过外层验证时写入 |
| 四级 | 海马状态或决策斜率的**独立估计** | 需可辨识性与外层验证双重支持 | **默认不写** |

### 5.2 三档截断并列敏感性分析（规则 D3，保留 v3 修订）

以 $c\in\{100,300,500\}$ 毫秒构造三个并列分析窗 $W_{\text{cog}}^{(c)}$，三档地位完全平等；三档差异是"动作成分侵入程度"的度量。

**决策规则 D3（截断一致性规则）**

> **预先规定**（不要求三档都显著）：
> 1. 仅当三档截断下的**效应方向一致**、且**效应量及其置信区间不存在实质性冲突**（三档效应量置信区间两两有重叠且符号一致）时，才允许把该效应作"**与认知相关**"的**谨慎**解释；
> 2. 三档之间的**显著性差异只作为功效信息报告**，不作为否决或解释的依据；
> 3. 若出现**方向翻转**，或效应量相互矛盾（置信区间无重叠），则降级为"**动作与认知成分混合**"，如实报告三档结果，不得只挑其中一档。

### 5.3 认知模型（描述性生成模型，三级基线）

**基线梯队（v4 新增，问题三必须比较三者）**：

| 编号 | 模型 | 参数 | 地位 |
|---|---|---|---|
| Q3-M0 | 常数反应时模型 | $RT=\mu+\varepsilon$（按记录分参数） | 最弱基线 |
| Q3-M1 | 简单分布模型 | 移位对数正态或伽马（按记录、按条件分参数） | 分布基线 |
| Q3-M2 | **竞争累积模型**（本方案二级主张） | $\beta,\lambda,\sigma,\Theta$、$z$ 时间结构、侧别偏置 $\delta$ | 正式候选 |

**竞争累积模型**：

$$dD_i=\big[\beta\,z_i(t)-\lambda\big]dt+\sigma\,dW_i,\qquad i\in\{L,R\}$$

- $z_i(t)$ 为位置证据（项目一提示后即可获得高位置先验，项目二需等目标出现）；
- 首次通过阈值 $\Theta$ 的时刻即反应时预测；
- 先到者决定点击侧；截止时间内均未到阈 → **未及时应答**；到达错误侧 → **错误应答**。

**该模型只用反应时与正确性即可拟合**，不需要脑电。**漂移扩散/竞争累积是标准模型（Ratcliff 1978；Usher & McClelland 2001），非原创。**

**可辨识性边界（冻结规则）**：本数据全部试次正确且无漏答，正确率不提供对阈值、扩散噪声、起点偏置等参数的独立约束。因此 Q3-M2 只允许作为**正确反应时分布的预测模型**。除非参数恢复模拟、剖面似然与外层留出预测同时通过，否则只报告可稳定预测的参数组合，不分别解释 $\sigma$、$\Theta$、起点偏置或“错误机制”；若恢复失败或相对 Q3-M1 无外层增益，必须降级到移位对数正态/伽马模型。

**准入条件**：Q3-M2 必须在外层留出数据上**同时优于 Q3-M0 与 Q3-M1**（对数预测密度、分位数误差、参数自助校准），否则退回较优基线并如实报告。

**拟合优度证据（保留 v3 修订）**：

- 主指标：留出数据上的**对数预测密度**；
- 辅助：**分位数误差**（模型预测分位数与实测分位数差的均方根）、**参数自助校准**（拟合参数生成重采样分布，检验实测分位数是否落在模拟分布 95% 区间内）；
- 分位数—分位数图仅作描述性展示；
- **不得**把同一数据拟合后的普通 KS p 值作为拟合优度证据。

**脑电弱耦合（条件性主张，保留）**：以问题二 M1 的模式轨迹为"脑电侧证据"，仅在以下**全部**条件满足时才写入结论：

1. 外层交叉验证下，单试次模式幅度/斜率对反应时的预测显著优于仅用截距的模型；
2. 该预测在四组数据上方向一致；
3. 三档截断满足规则 D3。

若任一条件不满足，则明确写"本数据不支持从单试次脑电估计认知状态参数"。

### 5.4 可检验预测（保留 v3 修订）

| 编号 | 预测 | 检验 | 结论措辞 |
|---|---|---|---|
| P1 | 反应时分布可由带条件依赖参数的竞争累积模型定量描述 | **留出对数预测密度、分位数误差、参数自助校准**（附分位数—分位数图） | 通过则写"可描述"，不写"因为位置不确定性" |
| P2 | 项目二与项目一反应时差在两名受试者内方向一致 | **每受试者内两记录的反应时分布差、稳健效应量、分位数差、自助置信区间；不置换任务标签** | 通过则写"受试者内、跨记录的描述性差异（含混杂）" |
| P3 | 项目二左目标较慢这一现象可由侧别偏置参数吸收 | 加入偏置后外层拟合改善且参数区间不含零 | 通过则写"可由侧别偏置描述"，不写机制原因 |
| P4 | **证伪性检验**：模型不应产生"提示方向 × 目标侧一致性"效应 | 一致性效应在两人身上符号相反 | 若模型预言一致性效应则模型设定错误，必须修正 |
| P5 | 错误与漏答分支 | 本数据 100% 正确、100% 有反应 | **明确写"该分支只有模型预测，无数据验证"** |
| P6 | 单试次脑电与反应时的关系 | 外层交叉验证 | 未通过则明确写"数据不支持" |
| P7 | 三档截断结论一致 | 规则 D3（方向一致 + 效应量无实质冲突） | 不一致则降级表述，显著性差异仅作功效信息 |

### 5.5 问题三的验证、消融与统计（保留，加三级基线）

**消融梯队**：Q3-M0（常数）→ Q3-M1（简单分布）→ Q3-M2（竞争累积）→ 加侧别偏置 → 加条件依赖的证据起点 → 加脑电弱耦合（逐级准入，每级必须外层改善且优于上一级）。

**统计**：任务差异按 6.5（记录层面描述性）；左右条件差异用块内受限标签置换（6.3）；模型拟合优度用留出对数预测密度、分位数误差与参数自助校准；模型比较用交叉验证对数似然与 AIC/BIC；跨受试者只做方向一致性检验。

**必须报告的阴性结果**：一致性效应不存在；错误与漏答不可验证；单试次脑电耦合是否成立（无论正负）。

---

## 6. 统计设计与验证规范（全方案强制，保留 v3）

### 6.1 嵌套连续试次块交叉验证

- **外层**：每记录按连续试次块（20 试次/块）分 5 折；外层只报告最终指标。
- **内层**：训练折内再分 4 折，选择处理链、阈值、模型复杂度。
- **禁止**：用全部数据选参数后再报告同批数据的显著性；用相邻重叠窗口同时进入训练与测试；用全局（跨试次）统计量做归一化。
- **报告**：内层选择稳定性（被选中折数）+ 外层最终指标，二者同时给出。
- **模型比较纪律**：所有模型（含基线 M0、Q3-M0/M1）必须**仅由训练折拟合、对相同测试折预测**；采用相同训练折、相同测试折，并报告参数数/有效自由度，保证基线自由度匹配。**禁止**把包含测试试次的经验平均曲线当作预测基线。

### 6.2 事件锁定检验：非平稳性下的受限替代

**零假设**：该成分与事件时刻无锁定关系。**为何不能用全记录整体循环移位**：原始信号含慢漂移与饱和段，**不能假定全记录严格平稳**。

**方案甲 试次级受限循环移位（默认）**：每试次事件时刻在其"试次专属允许区间"内循环移位；允许区间 = [试次起点+边距, 试次终点−边距]；**最小移位距离默认 1.5 秒**（1.0 秒分析窗 + 0.5 秒保护边距）；移位后分析窗不得与提示、目标、反应窗口重叠（各扩 0.2 秒保护边距）；不满足者拒绝重抽。

**方案乙 无事件区间替代时刻（并列）**：从无事件区间（引导段、尾段、试次间隙中与任何事件窗口至少相隔 1.5 秒的区间）均匀抽取；替代时刻的分析窗不得与任何事件窗口重叠；必须落在与真实事件**同一局部时间块**内（默认 ±60 秒）以控制漂移非平稳性。

**边界处理规则**：分析窗越出记录边界或落入相邻试次事件窗口者一律拒绝；不采用跨记录边界环绕；若采用循环移位须显式处理首尾相接并写入报告。

**适用前提（写入论文）**：方案甲要求试次间隔足以容纳移位窗（本数据满足）；方案乙要求可行替代时刻数**不少于事件数的 4 倍**，否则不可用；**两方案结论方向一致时方采用**；不一致时按 R18 降级（附录 C）。置换次数 10000。

### 6.3 条件差异检验：左右条件的块内受限标签置换

- **仅适用于同一记录内的左右条件比较**。
- **限制**：置换只在连续试次块内部进行（块内打乱标签），**保持每个时间块中的左右条件计数不变**，同时保持时间顺序与块结构。
- **零假设**：左右两条件的分布相同（给定每块左右计数结构）。
- **任务差异不在此列**：任务标签**不可在试次级置换**（见 6.5）。
- 报告要求：块长度、块数、块内计数是否保持、置换次数。

### 6.4 多重比较

- **主分析 1 个，题面驱动并在编码前冻结**（题面给定 250–500 毫秒范围；已采集额中电极 Fz；提示锁定正向响应），不校正；这属于方案冻结后的确认性主分析，**不得写成独立预注册**；
- **次要假设**用最大统计量法（通道 × 窗内时间点取 $\max|t|$），置换生成族错误率零分布，报告校正 p；
- **探索性分析**必须标注，不得作为结论。

### 6.5 任务差异：记录层面描述性比较

- **对象**：同一受试者的项目一记录与项目二记录的反应时分布；
- **报告内容**（每受试者内）：两记录的反应时分布差（中位数差）、稳健效应量、分位数差（25/50/75 百分位差）、**试次级自助置信区间**（2000 次）；
- **不做**：任务标签的试次级置换、任务差异的置换 p 值、因果推断；
- **必须并列报告混杂因素**：记录时段、疲劳、学习、注意力、记录质量（削顶数与坏试次比例的记录间差异）；
- **表述**：仅称"受试者内、跨记录的描述性差异"；两人方向一致时称"可复现的描述性差异"。

### 6.6 效应量的报告纪律

- 所有条件差异必须同时报告：效应量、置信区间、置换 p 值；
- 只报 p 值不报效应量视为不合格；
- 记录层面差异报告分布差、稳健效应量、分位数差与置信区间，不报告置换 p 值；
- 跨受试者只报方向一致性，不合并。

---

## 7. 相关方法与学术防撞车矩阵（v4 新增）

**总原则**：下列方法均为已有成果，本方案**不得将其重新命名为原创方法**；重合部分一律在正文认账并引用（附录 E）。"本方案仅针对本题新增的部分"只允许出现在第 8 章允许清单的范围内。

| 方法（标准名） | 已有方法解决什么 | 通道需求 | 是否处理长时间削顶 | 是否验证刺激特征保真 | 与本方案重合部分（认账） | 本方案仅针对本题新增的部分 |
|---|---|---|---|---|---|---|
| xDAWN（Rivet 2009） | 监督空间滤波增强事件相关电位、提高脑机接口分类信噪比 | 多通道协方差空间滤波；3 通道仍可估计，但空间自由度低，效果取决于正则化与数据质量 | 原文不包含削顶处理环节，依赖前置预处理 | 以分类可分性为准则（一类保真），但不检验事件锁定波形本身 | 以"左右可分性 AUC"作为外层保真指标之一 | 主链采用固定正交导联基、可靠度加权与饱和缺失化；不声称 xDAWN 不适用；工期允许时将正则化 xDAWN 作为补充敏感性基线 |
| 去噪源分离 DSS（Särelä & Valpola 2005；de Cheveigné & Simon 2008） | 用刺激锁定/重复性准则做空间滤波分离信号源与噪声 | 多通道（需估计空间滤波器；通道过少不可用） | 不专门处理削顶 | 以锁相/重复性为准则——与保真门控同源思想 | **保真门控的"事件锁定判定"思想即 DSS 思想**，必须引用并声明非原创 | 门控只做软阈值收缩、与可靠度加权和失败降级规则 D1 联合；不估计空间滤波器 |
| 伪迹子空间重构 ASR（Mullen 2015；Chang 2020）及其低密度应用（Kumaravel 2021） | 用干净段校准协方差、重建并剔除伪迹子空间 | 已有 8 通道低密度验证；不能据此推断 3 通道协方差校准同样稳定 | 可处理非平稳瞬态；现有低密度证据不能直接证明其能恢复本题近 2 秒平台削顶 | 以其自身统计阈值或外部任务响应评估，不等同于本题的事件保真约束 | 坏段标注、以"干净参考"校准的思想同源 | 主链不运行 ASR，但理由限定为“3 通道长平台场景缺少直接验证”，不写成一般性不适用结论；主链用传感器可靠度协方差与缺失观测 |
| 状态空间事件相关电位提取（de Munck 2004；Galka 2004；Das 2023） | 用最大似然/卡尔曼滤波或多层状态空间模型估计事件相关电位与噪声 | 存在多通道、单通道及多层形式 | 不专门处理饱和 | 以似然、估计精度或预测性能为准则 | 本方案 M1 的线性状态空间与卡尔曼递推即此类**标准方法**，必须引用并声明非原创 | 观测噪声协方差由可靠度场逐时刻调制；传感器级饱和行缺失化；加保真约束与可辨识性四项裁决 |
| 事件相关电位动态因果模型（David 2006） | 在源空间对事件相关电位做神经群因果模型比较与推断 | 多通道 + 导联场（lead field） | 不处理削顶 | 以贝叶斯模型比较为准则 | 仅有"生理解释与先验约束"层与之相似 | 本方案**明确不做源空间反演**；把该层次降级为理论说明与敏感性分析，不用于主张 |
| 漂移扩散/竞争证据累积模型（Ratcliff 1978；Usher & McClelland 2001） | 解释两选择任务的反应时与正确率分布 | 无通道需求（纯行为） | 不涉及脑电伪迹 | 以行为数据拟合为准则 | 问题三的竞争累积模型即**标准模型**，必须引用并声明非原创 | 留出对数预测密度/分位数误差/参数自助校准的拟合证据；三级基线准入；与脑电弱耦合的准入条件 |
| 严格交叉验证下的脑电预处理与解码（Varoquaux 2017；Engemann & Gramfort 2015；Kessler 2025） | 解码与预处理超参数选择的交叉验证纪律；量化预处理选择对解码与可解释性的影响 | 与通道数无关 | 不针对本题长削顶 | 说明单看解码性能可能利用结构化伪迹，不能替代神经信号保真验证 | 嵌套交叉验证纪律与预处理—解码权衡均属已有工作，必须引用 | 内层"预测损失 + 保真约束"联合目标；外层一次性评估；失败降级规则 |
| 正交源导联/双极/拉普拉斯空间滤波（Hjorth 1975；McFarland 1997） | 头皮电位的正交化重参考与空间滤波 | 少通道亦可（源导联即为此设计） | 不处理削顶 | 不验证刺激保真 | 本方案固定空间基即此类**经典导联技术**的标准化正交表达，必须引用并声明非原创 | 与可靠度场、状态空间观测协方差调制联合使用 |

**使用纪律**：第 7 章矩阵须在论文"相关工作"一节以精简形式出现；每个"重合部分"的引用必须出现在正文相应位置（附录 E 给出位置清单）。

---

## 8. 创新点（收口）与创新主张允许/禁止清单（v4 重写）

### 8.1 核心创新（唯一主张，含统一数学表达）

**核心创新**（一句话）：

> 面向**三电极、长平台削顶、设备滤波不可用**的数据，建立"**事件语义解析 — 逐时刻观测可靠度 — 刺激特征保真约束 — 可辨识性裁决**"的一体化闭环。

**统一数学表达（四条）**：

**（1）可靠度场**：由饱和、平台、瞬态与跨通道异常形成逐通道逐时刻可靠度（3.2 节定义）：

$$w_k(t)=\big[h^{\text{sat}}_k(t)\cdot h^{\text{plat}}_k(t)\big]\cdot s^{\text{tr}}_k(t)\cdot s^{\text{cross}}_k(t)\ \in[0,1],\qquad k\in\{\mathrm{Fz},\mathrm{F3},\mathrm{F4}\}$$

**（2）可靠度进入传感器空间观测方程**：潜在状态在固定模式基上定义，但观测可靠度按 Fz/F3/F4 分别进入协方差；完全饱和传感器作**缺失观测、不强行插值**（4.3 节）：

$$\mathbf y_{A(t)}(t)=\mathbf M_{A(t),:}\mathbf z(t)+\mathbf a_{A(t)}(t)+\boldsymbol\varepsilon_{A(t)}(t),\quad
\mathbf R_{A(t)}(t)=\operatorname{diag}_{k\in A(t)}\!\left[\frac{\sigma_{0,k}^{2}}{\max(w_k(t),10^{-3})}\right],\quad A(t)=\{k:w_k(t)>0\}.$$

若 $A(t)=\varnothing$，只做预测步；$w_k(t)=0$ 的行不进入分母，也不进入观测更新。这样避免先投影再平均可靠度而掩盖单个传感器饱和。

**（3）带保真约束的内层选择**：内层模型选择以**预测损失最小**为目标，同时约束**事件响应可靠性**与**左右刺激可分性**不得低于保守基线（3.1、3.5 节）：

$$\theta^{*}=\arg\min_{\theta}\ \mathcal{L}_{\text{inner}}(\theta)\quad\text{s.t.}\quad \mathcal{R}(\theta)\ge \mathcal{R}_{\text{B1}},\quad \mathrm{AUC}(\theta)\ge \mathrm{AUC}_{\text{B1}}$$

其中 $\mathcal{R}$ 为事件锁定可靠性指标（如响应窗分半可靠性或事件锁定统计量），$\mathrm{AUC}$ 为块交叉验证左右可分性；约束不可行即触发降级规则 D1。

**（4）外层一次性评估**：外层数据只用于**一次**最终评估，**不参与**可靠度、阈值与模型选择（3.1、6.1 节）。

**明确认账（答辩必答）**：

- 可靠度加权卡尔曼估计：**标准技术**（加权最小二乘/卡尔曼滤波，Kalman 1960；缺失观测处理，Shumway & Stoffer 1982），**非原创**；
- 状态空间模型：**标准技术**（预测误差法/卡尔曼似然），**非原创**；
- 交叉验证（含嵌套、防泄漏）：**标准方法学**（Varoquaux 2017；Engemann & Gramfort 2015），**非原创**；
- 固定正交空间基：**经典导联技术**（Hjorth 1975；McFarland 1997），**非原创**；
- 漂移扩散/竞争累积：**标准行为模型**（Ratcliff 1978；Usher & McClelland 2001），**非原创**；
- 保真门控的事件锁定思想：**去噪源分离已有**（de Cheveigné & Simon 2008），**非原创**。

**真正的创新在于**：针对本题的数据缺陷（三电极、±1000 长平台削顶、第 4–6 通道不可用、两任务第 9 通道语义不同），将上述标准组件与**刺激特征保真约束**及**失败降级规则**（D1/D2/D3、R15/R16 裁决）联合成一个可检验的闭环，并给出与该缺陷一一对应的具体设计（饱和缺失化而非插值、可靠度进协方差、保真约束的目标函数、恢复失败参数不解释）。此外，"第 9 通道跨任务语义的严格解析"（第 2 章）是本方案独有的数据发现。

### 8.2 创新主张允许与禁止清单

**允许（只允许主张以下四条）**：

| 编号 | 允许主张 | 边界 |
|---|---|---|
| A1 | 本题特定的联合框架：事件语义解析—可靠度—保真约束—可辨识性裁决的闭环 | 框架联合为创新；各组件非原创且逐一引用 |
| A2 | 逐时刻可靠度进入观测协方差、完全饱和点作缺失观测不插值 | 针对 ±1000 长平台削顶的具体设计 |
| A3 | 保真约束（可靠性、可分性不劣于保守基线）与失败降级机制（D1/D2/D3、R15/R16） | 以第 3、5、6 章规则为准 |
| A4 | 第 9 通道跨任务语义的严格解析（±1 段终点即反应时刻；项目一无 ±2） | 以第 2 章证据为准 |

**禁止（以下表述不得出现在论文）**：

| 编号 | 禁止主张 | 原因 |
|---|---|---|
| F1 | 宣称首创状态空间事件相关电位模型 | 已有 de Munck 2004、Galka 2004 等 |
| F2 | 宣称首创事件锁定去噪 | 去噪源分离（DSS）已有该思想 |
| F3 | 宣称固定正交空间基是新算法 | 经典源导联/双极/拉普拉斯技术已有（Hjorth 1975） |
| F4 | 宣称从三个额区电极定位外侧膝状体核、视觉皮层或海马 | 3 电极不可辨识，超出数据支撑 |
| F5 | 宣称任务间差异具有因果意义 | 记录层面混杂（时段、疲劳、学习、记录质量），只能描述性报告 |

---

## 9. 竞赛主线与图表压缩（v4 新增）

### 9.1 正文主线（固定，不扩展）

1. **问题一**：事件语义解析与可靠度加权视觉响应提取（四基线对比 + 保真约束 + D1 降级）；
2. **问题二**：固定可观测空间模式上的输入—状态—观测模型及左右差异检验（M0 有限脉冲响应基线 → M1 → H1/H2 检验）；
3. **问题三**：行为反应时生成模型（三级基线），以及**只有通过外层验证才准入**的脑电弱耦合（规则 D3 三档截断）。

### 9.2 正文图压缩至最多 9 张

| 编号 | 图名 | 服务论证 |
|---|---|---|
| 图 1 | 数据质量与事件语义核验（削顶/平台/坏试次 + 事件链 + 反应时分布，四文件，按记录分列） | 第 1、2 章事实 |
| 图 2 | 问题一四基线（B1/B2/B3/B4）的外层验证对比 | 可靠度加权提取是否成立（D1） |
| 图 3 | 提示锁定事件相关曲线与受限替代零分布 | 视觉响应是否存在（R5/R18） |
| 图 4 | 左右差异与最大统计量零分布（块内受限置换） | 条件差异显著性（R6） |
| 图 5 | 固定正交导联基的三模式时间序列与置信带 | 模式分解有效性（R7） |
| 图 6 | 主模型 M1 对留出块的预测与残差（对比训练折拟合 M0） | 状态空间模型是否优于基线（R9） |
| 图 7 | 左右差异两种解释（增益 vs 时序）的外层比较 | 机制是否可区分（R11） |
| 图 8 | 反应时三级基线（常数/简单分布/竞争累积）的留出拟合对比 | 行为模型准入（R12） |
| 图 9 | 认知结论准入证据：三档截断效应方向与置信区间 + 单试次耦合外层验证 | 认知解释准入（R13/R14） |

### 9.3 移入附录的内容

- **附录 C**：完整风险登记表（18 条，含 R15 参数恢复模拟、R16 基线泄漏审计、R17 记录层面任务混杂、R18 非平稳性替代检验）；
- **附录 D**：可辨识性诊断材料（剖面似然、费舍尔信息谱、恢复模拟散点）、扩展模型 E1–E5 的完整设定与结果、失败结果的存档（被否决候选、恢复失败参数、泄漏审计记录、xDAWN/ASR 适用边界论证）；
- **正文只保留**：主要阴性结论、可解释参数清单、模型选优结论与降级结论。

### 9.4 正文必须保留的主要阴性结论

1. 100% 有反应、100% 点击与目标同侧 → 错误与漏答分支无数据验证；
2. 提示方向与目标侧一致性效应不存在（两人符号相反）；
3. 第 4–6 通道与原始信号相关性低（0.09–0.71），不可用作脑电或噪声参考；
4. 任务间反应时差仅为受试者内、跨记录描述性差异（含混杂清单，不置换任务标签、不作因果）；
5. 朴素流程 250–550 毫秒峰值 t 值约 1.0–2.5（若主分析未通过则报告"未检出"）；
6. 单试次脑电耦合与海马/决策斜率估计的最终判定（无论正负，必须写明）；
7. 三档截断不一致时降级为"动作与认知混合"的判定（若发生）；
8. 恢复失败的参数清单（若有）。

---

## 10. 写作边界（保留并增补）

1. **不做诊断性表述**：2 名受试者、单一视觉认知任务，不能推出精神分裂症、阿尔茨海默症或任何临床结论；相关背景只作模型可扩展性的讨论。
2. **不做人群推断**：所有统计结论限定在"本受试者、本任务、本记录"内；跨受试者只报方向一致性。
3. **不宣称源定位**：外侧膝状体核、视觉皮层、海马只作生理解释与先验约束，本文不提供任何源定位结论。
4. **不宣称中央—顶区 P300**：只称"候选正向事件相关响应窗"，并报告实际潜伏期区间。
5. **不把分类准确率当主体**：分类只用于检验特征可分性。
6. **不隐藏阴性结果**：一致性效应不存在、错误与漏答不可验证、参数不可辨识或恢复失败、去噪候选被否决、单试次耦合不成立——全部必须写入正文。
7. **不做因果推断**：任务间反应时差只作受试者内、跨记录的描述性差异，并列明混杂因素；**不置换任务标签、不报任务差异的置换 p 值**。
8. **不用自设编码层证明数据结论**：刺激编码层只构造外部输入，左右差异只由观测数据的幅度、潜伏期、侧化模式检验。
9. **不解释恢复失败的参数**：凡参数恢复模拟判为失败的增益、延迟、时间常数，一律不写入任何机制解释。
10. **不以同一数据拟合的 KS p 值为拟合证据**：反应时模型拟合优度只依据留出对数预测密度、分位数误差与参数自助校准。
11. **不把已有方法重新命名为原创方法**（v4 新增）：凡第 7 章矩阵列出的方法及其重合部分，一律按标准名称引用原始/代表性文献。

---

## 11. 后续编码任务：文件结构、模块顺序与输出物（v4 微调）

### 11.1 目录结构

```
pro-mathematical-modeling/
├── configs/
│   └── cti.yaml                 # 采样率、事件阈值、窗口、质量阈值、交叉验证折数、随机种子、恢复模拟阈值
├── Ques1/
│   ├── __init__.py
│   ├── events.py                # 事件解析、试次表、侧别标签、反应时刻
│   ├── quality.py               # 饱和/平台/瞬态/不一致标注与质量总览
│   ├── reliability.py           # 可靠度场 w_k(t)（v4 新增）
│   ├── baselines.py             # B1/B2/B3 保守基线（v4 新增）
│   ├── denoise.py               # B4 可靠度加权观测模型、滤波、保真门控
│   ├── cardiac.py               # 心拍对齐检验与滞后回归
│   ├── erp.py                   # 可靠度加权稳健平均、置信带、参数化拟合、单试次特征
│   ├── validate.py              # 嵌套交叉验证、替代事件检验、块内受限置换、多重比较
│   └── run_q1.py
├── Ques2/
│   ├── __init__.py
│   ├── basis.py                 # 固定正交导联基（m1/m2/m3）与模式投影
│   ├── model.py                 # M1 线性三模式状态空间（含可靠度观测）+ E1–E5 + M0（训练折拟合 FIR）
│   ├── identify.py              # 预测误差法/极大似然、剖面似然、费舍尔信息
│   ├── simulate.py              # 参数恢复模拟（R15）
│   ├── features.py              # 单试次特征
│   ├── decode.py                # 外层交叉验证分类
│   ├── compare.py               # M0–E5 同折模型比较、泄漏审计（R16）
│   └── run_q2.py
├── Ques3/
│   ├── __init__.py
│   ├── rt_model.py              # 三级基线：常数 / 简单分布 / 竞争累积（留出拟合指标）
│   ├── eeg_coupling.py          # 脑电弱耦合（条件准入）
│   ├── truncation.py            # 三档截断并列分析与规则 D3
│   ├── validate.py              # 外层验证、P1–P7 检验
│   └── run_q3.py
├── tools/
│   ├── io_mat.py, stats.py, plotting.py   # 读取、统计、绘图
│   └── remote_tool.py           # 既有工具（本方案不需要）
├── outputs/                     # 图、表、中间产物（交付后清理中间文件）
├── version.md
├── C题分析.md                    # 既有文件，不修改
├── C题高分完整方案.md             # v1，不修改
├── C题高分完整方案_v2.md          # v2，不修改
├── C题高分完整方案_v3.md          # v3，不修改
└── C题高分完整方案_v4.md          # 本文件
```

### 11.2 模块顺序与验收标准

| 步骤 | 模块 | 关键动作 | 验收标准 |
|---|---|---|---|
| 0 | 版本控制 | 编码前先提交 | 有提交记录 |
| 1 | `Ques1/events.py` | 事件解析与试次表 | 2.4 节 10 条断言全通过 |
| 2 | `Ques1/quality.py` | 质量标注与总览 | 表 1 产出，记录间质量对比（R17） |
| 3 | `Ques1/reliability.py` | 可靠度场 $w_k(t)$ | 与 8.1 公式一致；阈值仅由训练折确定 |
| 4 | `Ques1/baselines.py` + `denoise.py` + `validate.py` | B1–B4 与嵌套交叉验证；替代事件检验 | 表 2 填满；规则 D1 给出裁决；R18 两方案一致 |
| 5 | `Ques1/cardiac.py` + `erp.py` | 心电两步验证；曲线与特征 | 规则 D2 裁决；图 3、图 4 产出 |
| 6 | `Ques2/basis.py` | 固定正交导联基与模式投影 | 图 5 产出，模式能量与噪声比较 |
| 7 | `Ques2/model.py` + `identify.py` + `simulate.py` | M1 与训练折拟合 M0、四项证明、恢复模拟 | 图 6 产出；R10/R15 结论明确；诊断材料入附录 D |
| 8 | `Ques2/compare.py` + `decode.py` | 同折模型比较、泄漏审计、可分性 | 图 7 产出；R16 审计通过 |
| 9 | `Ques3/rt_model.py` | 三级基线（常数/简单分布/竞争累积）留出拟合 | 图 8 产出，Q3-M2 准入裁决 |
| 10 | `Ques3/truncation.py` + `eeg_coupling.py` + `validate.py` | 三档截断与弱耦合 | 图 9 产出；规则 D3 给出裁决 |
| 11 | 出图与写作 | 正文 9 图 + 附录图表规范化 | 图表与风险登记表（18 条，附录 C）逐条对应 |

### 11.3 输出物

**数据类**：`trials.csv`、`quality_report.csv`、`reliability.npz`、`cardiac_report.csv`、`erp_curves.npz`、`single_trial_features.csv`、`q2_params.csv`、`q2_identifiability.csv`、`q2_recovery.csv`、`rt_fit.csv`、`truncation_report.csv`、`risk_status.csv`（18 条执行状态与泄漏审计标记）。

**图表类**：正文图 1–9、正文表 1–3（事件与质量核验、问题一基线对比、问题二模型比较）；附录图 A1–A3、附录表 C1（完整风险登记）、D1（参数与恢复模拟）。

**文档类**：论文正文、附录（参数表、可辨识性诊断、恢复模拟、防撞车矩阵精简版、参考文献计划）、`version.md`。

### 11.4 工程约束与算力

- 遵守项目规则：代码不留中文注释；代码极简；中间与临时产物用后即删；编码前先提交；对用户回答不中英文夹杂；虚拟环境 `kenny_ok`；版本级改动写入 `version.md`。
- **不需要远程训练**：数据规模为 4 文件 × 约 23 万采样点 × 3 通道；主模型为线性状态空间（卡尔曼递推似然）；全部计算在本机（i5-14500 + RTX3050）完成。
- 所有随机过程固定种子；交叉验证折划分与恢复模拟阈值一旦确定即写入配置，不得在报告阶段更改。

### 11.5 建议时间安排（5 天）

| 时间 | 任务 |
|---|---|
| 第 1 天 | 步骤 1–3：事件链、质量标注、可靠度场 |
| 第 2 天 | 步骤 4–5：四基线裁决（D1、R18）与事件相关曲线 |
| 第 3 天 | 步骤 6–8：固定导联基、M1 与 M0、四项证明与泄漏审计 |
| 第 4 天 | 步骤 9–10：三级基线反应时模型、三档截断、弱耦合检验 |
| 第 5 天 | 步骤 11：统稿、正文 9 图规范化、风险登记（18 条）与参考文献计划逐条复核 |

---

## 附录 A 关键参数默认值（v4）

| 参数 | 默认值 | 说明 |
|---|---|---|
| 采样率 | 256 赫兹 | 数据给定 |
| 通道顺序 | Fz、F3、F4 | 空间基定义的前提，全文档统一 |
| 提示后分析窗 | $[-0.5,+1.0]$ 秒 | 问题一 |
| 主分析（题面驱动、编码前冻结） | 250–500 毫秒，Fz，正向峰 | 唯一，不校正；不得宣称独立预注册 |
| 基线窗 | $[-0.5,-0.2]$ 秒 | 逐试次稳健基线 |
| 带通 | 0.3 赫兹（内层可在 0.1/0.3/0.5 中选）–35 赫兹，零相位 | 内层选择，外层裁决 |
| 带阻 | 50 赫兹零相位 | 非主要矛盾 |
| 饱和判据 | $\lvert x\rvert=1000$ | 可靠度硬因子 $h^{\text{sat}}=0$ |
| 平台判据 | 连续相同值 $\ge3$ 点 | 可靠度硬因子 $h^{\text{plat}}=0$ |
| 瞬态软因子 | $\tau_{\text{tr}}=8\times$ 差分中位绝对偏差 | $s^{\text{tr}}=1/(1+(\lvert\Delta x\rvert/\tau_{\text{tr}})^2)$ |
| 跨通道软因子 | $\tau_{\text{cross}}=6\times$ 残差中位绝对偏差 | $s^{\text{cross}}=1/(1+(r/\tau_{\text{cross}})^2)$ |
| 正可靠度数值下限 | $w_{\min}=10^{-3}$ | 仅用于 $0<w\le1$ 的方差计算；$w=0$ 时删除该传感器观测行，绝不以 $w_{\min}$ 代替 |
| 短段插补上限（仅 B3 基线） | 0.1 秒 | 长于 0.2 秒不修复（B3 对照用） |
| 交叉验证 | 外层 5 折 × 20 连续试次/块；内层 4 折；模型比较同折 | 折划分写入配置 |
| 置换次数 | 10000（替代事件/块内受限标签）/ 1000（标签零分布） | 三者不可混用 |
| 自助次数 | 2000 | 参数与曲线置信带；记录层面分布差区间 |
| 空间基 | $\mathbf m_1=(1,1,1)/\sqrt3$、$\mathbf m_2=(0,1,-1)/\sqrt2$、$\mathbf m_3=(2,-1,-1)/\sqrt6$ | 经典导联技术，非原创 |
| 模式数上限 | 3（可降为 2） | 电极数决定的秩限制 |
| 延迟参数 | 跨模式共享，先验中心 60 毫秒 | 剖面似然若过宽则固定 |
| 皮层时间常数先验 | $\tau_E$ 5–20 毫秒、$\tau_I$ 10–40 毫秒 | 仅作先验范围与生理解释 |
| 替代事件最小移位距离 | 1.5 秒（= 1.0 秒分析窗 + 0.5 秒保护边距） | 写入配置 |
| 替代时刻局部时间块 | ±60 秒 | 控制漂移非平稳性 |
| 恢复模拟判定 | 秩相关 < 0.5、或覆盖率 < 60%、或 > 99% 判失败 | 失败参数不解释 |
| 截断档位 | 100 / 300 / 500 毫秒 | 并列；规则 D3（方向一致 + 效应量无实质冲突）裁决 |

---

## 附录 B 一页纸路线图（v4）

```
事件语义层（全方案前置）
  第8通道上升沿 = 试次零点
  第9通道：项目一 ±1 段[目标呈现 -> 反应]，无 ±2（段终点即反应时刻）
            项目二 ±1 段[目标呈现 -> 反应] + 单点 ±2 = 反应时刻
  => trials.csv（10 条断言，固定种子复现）

问题一（事件语义解析 + 可靠度加权提取）
  质量标注 -> 可靠度场 w_k(t)（饱和/平台=0；瞬态/跨通道=软衰减）
  -> 四基线：B1 带通+稳健平均 | B2 硬剔除 | B3 短段插补+长段降权 | B4 可靠度加权观测模型
  -> 内层选择 = 预测损失最小 且 可靠性、可分性不劣于保守基线（否则 D1 降级）
  -> 外层一次性评估；心电污染用「心拍对齐 + 滞后回归」验证（D2）
  -> 主分析：Fz、250-500 毫秒（题面驱动、编码前冻结，非独立预注册）；最大统计量控制族错误率
  -> 事件锁定 = 受限替代事件检验；左右差异 = 块内受限标签置换；任务差异不置换标签
  （xDAWN、ASR 仅作文献边界讨论；不写成算法不适用，正则化 xDAWN 可作补充敏感性基线）

问题二（固定可观测空间模式上的输入-状态-观测模型）
  固定正交导联基（通道序 Fz、F3、F4）：m1 共模 / m2=(F3-F4)/√2 左右差模 / m3 额中对比模
  M1：线性三模式状态空间（E-I 二阶 + 两路事件输入 + 伪迹项 + 可靠度观测协方差，饱和=缺失）
  基线 M0：训练折拟合的正则化 FIR（同训练折、同测试折，禁止含测试试次）
  -> 留出预测 M1 vs M0 -> E1(降参) E2(两模式) E3(条件增益/延迟) E4(耦合) E5(非线性) [附录 D]
  -> 左右差异 = H1(增益) vs H2(时序)；不可区分时如实报告
  -> 可辨识性四项证明：留出预测 + 剖面似然 + 费舍尔信息 + 参数恢复模拟（失败参数不解释）
  -> 状态空间/卡尔曼/交叉验证/固定基均为标准技术，逐一引用（防撞车矩阵）

问题三（行为反应时生成模型 + 条件准入的脑电弱耦合）
  三级基线：常数反应时 | 简单分布（移位对数正态/伽马） | 竞争累积（标准模型）
  -> 竞争累积须在留出对数预测密度、分位数误差、参数自助校准上同时优于前两者
  -> 任务差异 = 记录层面描述性（分布差、稳健效应量、分位数差、自助区间；不置换标签、不因果）
  -> 三档截断 100/300/500 毫秒并列；D3：方向一致 + 效应量无实质冲突才谨慎解释为认知相关
     显著性差异仅作功效信息；矛盾则降级为动作与认知混合
  -> 脑电弱耦合仅在通过外层验证时写入；海马/决策斜率默认不写
  -> 证伪性检验 P4：模型不应产生「提示方向 × 目标侧一致性」效应

贯穿全案
  核心创新 = 事件语义解析—可靠度—保真约束—可辨识性裁决的联合闭环（组件均非原创）
  风险登记 18 条（附录 C）；正文 9 图；主要阴性结论留在正文
```

---

## 附录 C 可复现性与风险登记（完整 18 条，v4 移入附录）

### C.1 数值的复现状态

| 内容 | 当前状态 | 复现要求 |
|---|---|---|
| 采样点计数、时长、提示次数 | 待正式固定随机种子脚本复现 | 输出 `outputs/q1/trials.csv` 与核验报告 |
| 削顶计数、平台长度、坏试次比例 | 待复现 | 输出 `outputs/q1/quality_report.csv` |
| 反应时中位数与任务差异描述量 | 待复现 | 输出 `outputs/q3/rt_fit.csv`（分布差、稳健效应量、分位数差、自助区间） |
| 事件结构断言的试次编号（如 B 项目一不一致试次） | 待复现 | 断言集全部通过 |
| 第 7 通道相关值 | 待复现，且**仅作筛查** | 心拍对齐与滞后回归结果 |
| 可靠度场与 B4 提取结果 | 待执行 | 输出 `outputs/q1/reliability.npz` 与表 2 |
| 参数恢复模拟结果 | 待执行 | 输出 `outputs/q2/recovery.csv` |
| 基线泄漏审计记录 | 待执行 | 输出 `outputs/risk_status.csv` 中的审计标记 |
| 全部图表数值 | 待复现 | 图表由脚本直接生成，不手工填写 |

**强制要求**：正式脚本固定随机种子并写入配置；文档中所有数字不得手工转录，必须由图/表生成脚本的输出来源。

### C.2 风险登记表（18 条）

| 编号 | 主张 | 证据来源 | 所需图表 | 失败判据 | 失败后的降级方案 |
|---|---|---|---|---|---|
| R1 | 第 9 通道在项目一/二语义不同 | 标签字符串 + 段长结构 + ±2 与段终点重合 | 图 1、表 1 | 任一文件 ±2 与段终点不一致 | 逐文件重新定义反应时刻；双标号敏感性 |
| R2 | 四文件质量分级不同 | 削顶/平台/坏点统计 | 图 1、表 1 | 复现计数与文档显著不符 | 重新审计并更新图表；以复现为准 |
| R3 | B4 优于保守基线 | 嵌套交叉验证外层指标（B1–B4） | 图 2、表 2 | 外层 AUC 或分半可靠性未改善 | 退回最优保守基线（B1/B2/B3），按 D1 如实报告 |
| R4 | 保真门控有效 | 受限替代事件检验 + 外层指标 | 图 2 | 被保护成分的改变不影响力结论 | 去掉门控，只保留替代事件检验 |
| R5 | 提示锁定正向响应存在 | 最大统计量 + 受限替代事件检验 | 图 3、表 2 | 主分析 p>0.05 或 5 折不稳定 | 报告"未检出"；只报告置信带与描述性波形 |
| R6 | 左右差异存在 | 差模 $\mathbf m_2$ 分量 + 块内受限标签置换 | 图 4 | 校正后 p>0.05 | 报告阴性；单试次 AUC 作为唯一可分性证据 |
| R7 | 三模式分解有意义 | 模式能量与噪声比较 | 图 5 | 第 3 模式能量不显著 | 降为两模式（E2） |
| R8 | 任务间反应时差可复现（描述性） | **每受试者内分布差、稳健效应量、分位数差、自助置信区间（不置换任务标签）** | 图 1、图 8 | 复现后两人方向不一致 | 降级为描述性观察，不构成效应 |
| R9 | 主模型优于基线 | 外层留出块预测误差 vs **训练折拟合的 M0**（同折） | 图 6、表 3 | 未优于 M0 | 退回 M0，只报告描述性拟合 |
| R10 | 关键参数可辨识 | 剖面似然 + 费舍尔信息 + 恢复模拟 | 附录图 A1 | 剖面似然平坦或恢复失败 | 固定参数，只报告可辨识组合 |
| R11 | 左右差异的增益/时序解释 | 外层预测 + 参数区间 | 图 7 | 两解释不可区分 | 报告不可辨识，只给描述性差异（幅度/潜伏期/侧化） |
| R12 | 竞争累积模型优于简单基线 | **留出对数预测密度、分位数误差、参数自助校准** | 图 8 | 留出指标未优于 Q3-M0/M1 | 退回较优基线，只报分布差与置信区间 |
| R13 | 三档截断结论一致 | 三档并列分析（规则 D3：方向一致 + 效应量无实质冲突） | 图 9 | 任一档方向翻转，或效应量置信区间实质性冲突 | 按规则 D3 降级为"动作与认知混合"，如实报告三档（显著性差异仅作功效信息） |
| R14 | 单试次脑电与反应时相关 | 外层交叉验证 | 图 9 | 未优于截距模型 | 明确写"数据不支持单试次认知参数估计" |
| R15 | 参数恢复模拟 | 已知真值模拟试次 + 同流程重拟合的偏差/秩相关/覆盖率 | 附录图 A1 | 任一参数秩相关 < 0.5、或覆盖率 < 60%、或 > 99% | 该参数固定为先验值并在表 3 标"不可解释"，不得写入机制结论 |
| R16 | 基线泄漏审计 | 训练/测试折划分配置 + 各阶段数据依赖审计记录 | 附录表 C1 | 任一处测试试次影响基线/模板/归一化（如经验平均含测试试次） | 修复泄漏并重跑全部下游分析；无法修复时相关结论降为描述性并声明 |
| R17 | 记录层面任务混杂 | 两任务记录的质量指标（削顶数、漂移水平、存活试次）与时间进程对比 | 图 1（按记录分列） | 质量指标在任务间实质不同且方向可解释反应时差 | 任务差异仅作描述性跨记录差异，正文列出混杂清单，不作任何因果表述 |
| R18 | 非平稳性下的替代事件检验 | 方案甲（试次级受限循环移位）与方案乙（无事件区间替代时刻）结果 | 图 3（零分布面板） | 两方案结论方向不一致，或可行替代时刻数 < 4×事件数 | 事件锁定结论标"不可解释"，改由外层留出预测支持；仅报告描述性波形 |

---

## 附录 D 扩展模型与失败结果的附录存放说明

正文只保留主线结论；以下材料一律放附录，不进入正文图表限额：

- **D1 可辨识性诊断材料**：逐参数剖面似然曲线、费舍尔信息矩阵特征值谱与条件数、参数恢复模拟散点（真值 vs 估计、覆盖率表）——对应附录图 A1；
- **D2 扩展模型 E1–E5**：每个扩展模型的方程补充、准入检验结果、未通过扩展的完整记录——对应附录图 A2；
- **D3 失败结果存档**：被否决的候选方案（如 B4 未通过、门控无效、E3/E4/E5 未改善）、恢复失败参数清单、基线泄漏审计记录、三档截断不一致时的完整三档结果——对应附录图 A3 与附录表 C1；
- **D4 xDAWN 与伪迹子空间重构的适用边界论证**：xDAWN 在 3 通道下仍可估计，但空间自由度低且易受削顶污染；ASR 已有 8 通道证据，但不能直接外推到 3 通道长平台。二者不纳入主链的理由必须写成“缺少本场景直接验证与工期约束”，不得写成算法普遍无效；若实现补充正则化 xDAWN，只进入敏感性分析（引用见附录 E）。

---

## 附录 E 正式参考文献计划（v4 新增）

**使用纪律**：

1. 本表只列"应引计划"；**所有条目须经文献管理器逐条核对后再写入论文，禁止凭本表直接复制引用格式**；
2. 本表**不虚构任何文献、作者、题目或结论**；标注"题目给定"的条目来自竞赛题目自带参考文献，仅按题目原文转引；
3. 第 7 章矩阵中的每个"重合部分"必须在正文对应位置引用下表文献。

| 编号 | 组件（标准名） | 应引文献（作者、年份、期刊；核对后补全卷页） | 正文引用位置 |
|---|---|---|---|
| 1 | xDAWN 空间滤波 | Rivet, Souloumiac, Attina & Gibert (2009), IEEE Trans. Biomed. Eng. 56(8) | 第 7 章矩阵、第 3 章文献基线 |
| 2 | 去噪源分离（DSS） | Särelä & Valpola (2005), J. Mach. Learn. Res. 6；de Cheveigné & Simon (2008), J. Neurosci. Methods 171(2) | 第 7 章矩阵、第 3 章保真门控 |
| 3 | 伪迹子空间重构（ASR） | Mullen et al. (2015), IEEE Trans. Biomed. Eng. 62(11)；Chang et al. (2020), IEEE Trans. Biomed. Eng. 67(4)；Kumaravel et al. (2021), EMBC, DOI: 10.1109/EMBC46164.2021.9629771 | 第 7 章矩阵、第 3 章文献边界 |
| 4 | 状态空间/最大似然逐试次事件相关电位提取 | de Munck et al. (2004), IEEE Trans. Biomed. Eng. 51(12)；Galka et al. (2004), NeuroImage 23(2)；Das, He & Purdon (2023), Asilomar, DOI: 10.1109/IEEECONF59524.2023.10476951 | 第 4 章主模型、第 7 章矩阵、第 8 章认账 |
| 5 | 事件相关电位动态因果模型 | David et al. (2006), NeuroImage 30(4) | 第 7 章矩阵、第 4 章生理解释降级处 |
| 6 | 漂移扩散模型 | Ratcliff (1978), Psychol. Rev. 85(2)；Ratcliff & McKoon (2008), Neural Comput. 20(4) | 第 5 章竞争累积模型、第 8 章认账 |
| 7 | 竞争证据累积模型 | Usher & McClelland (2001), Psychol. Rev. 108(3) | 第 5 章竞争累积模型 |
| 8 | 严格交叉验证下的解码/预处理 | Varoquaux et al. (2017), NeuroImage 145；Engemann & Gramfort (2015), NeuroImage 108；Kessler, Enge & Skeide (2025), Communications Biology 8:1039, DOI: 10.1038/s42003-025-08464-3 | 第 3、6 章交叉验证纪律与第 7 章预处理—解码权衡 |
| 9 | 正交源导联（头皮电位正交化） | Hjorth (1975), Electroenceph. Clin. Neurophysiol. 39(5) | 第 4 章固定空间基、第 8 章认账 |
| 10 | 脑电通信空间滤波器（双极/拉普拉斯等） | McFarland, McCane, David & Wolpaw (1997), Electroenceph. Clin. Neurophysiol. 103(3) | 第 4 章固定空间基 |
| 11 | 卡尔曼滤波 | Kalman (1960), J. Basic Eng. 82(1) | 第 4 章拟合方法、第 8 章认账 |
| 12 | 状态空间缺失观测/EM 估计 | Shumway & Stoffer (1982), J. Time Ser. Anal. 3(4) | 第 4 章饱和缺失化处理 |
| 13 | 稳健位置估计 | Huber (1964), Ann. Math. Statist. 35(1) | 第 3 章稳健平均 |
| 14 | 自助法 | Efron (1979), Ann. Statist. 7(1) | 第 3、5、6 章置信区间 |
| 15 | 簇级置换检验 | Maris & Oostenveld (2007), J. Neurosci. Methods 164(1)；Nichols & Holmes (2002), Hum. Brain Mapp. 15(1) | 第 6 章置换检验与多重比较 |
| 16 | 结构/实际可辨识性的剖面似然分析 | Raue et al. (2009), Bioinformatics 25(15) | 第 4 章可辨识性诊断 |
| 17 | 计算模型的选择、验证与参数恢复纪律 | Wilson & Collins (2019), eLife 8 | 第 4、5 章模型比较与恢复模拟 |
| 18 | 神经群模型（兴奋—抑制） | Wilson & Cowan (1972), Biophys. J. 12(1) | 第 4 章生理解释/敏感性分析 |
| 19 | 独立成分分析用于脑电（说明 3 通道下为何不采用） | Makeig, Bell, Jung & Sejnowski (1996), NIPS 8 | 第 1 章陷阱清单第 9 条 |
| 20 | P300 经典描述（说明额区观察窗与中央—顶区 P300 的差别） | Sutton, Braren, Zubin & John (1965), Science 150 | 第 3 章表述纪律 |
| 21 | 自动化伪迹检测指标（FASTER） | Nolan, Whelan & Reilly (2010), J. Neurosci. Methods 192(1) | 第 3 章质量标注 |
| 22 | 标准化脑电预处理管线（PREP） | Bigdely-Shamlo et al. (2015), Front. Neuroinform. 9 | 第 3 章管线设计、第 7 章矩阵 |
| 23 | 题目给定文献 [1]：脑电计算模型综述 | Glomb et al. (2022), Brain Topogr. 35 | 绪论 |
| 24 | 题目给定文献 [4]：藏本模型综述 | Acebrón et al. (2005), Rev. Mod. Phys. 77(1) | 绪论（宏观模型背景） |
| 25 | 题目给定文献 [5]：形状组合特征（COSFIRE） | Azzopardi & Petkov (2014), Front. Comput. Neurosci. 8 | 第 4 章刺激编码层理论讨论（附录） |
| 26 | 题目给定文献 [2]、[3]：长程连接统计模型；海马相位幅度耦合 | 按题目原文转引，核对后使用 | 绪论与生理解释处（仅背景） |

---

**本方案的四条自检底线（v4 更新）**

1. 任何一步处理，必须同时报告"噪声下降"与"刺激特征保留"两组指标；只报前者视为未完成。
2. 任何机制解释，必须给出与之竞争的备择解释，并说明用什么观测量区分；不可区分时如实报告，不得挑一个写成结论。
3. 任何超过 3 个可观测空间模式、或需要单试次可靠估计的主张，默认视为不可辨识/不可验证，除非通过外层连续试次块验证。
4. 任何参数在通过参数恢复模拟之前不得解释；任何已有方法（第 7 章矩阵所列）一律按标准名称引用，不得重新命名为原创方法。

---

## 附录 F 冻结执行规则（2026-09-23）

1. **唯一基线**：后续编码、调试、实验登记与论文写作只以本文件为设计依据；`C题分析.md`、无后缀版、v2、v3 仅作历史材料，不得覆盖本文件规则。
2. **冻结项**：事件语义与反应时定义、训练/验证/外层测试的连续块划分原则、题面驱动主分析、B1–B4 与 M0–M1/Q3-M0–M2 的比较关系、传感器空间可靠度观测方程、所有准入/降级规则、学术主张边界均不得因结果好坏而改动。
3. **允许的实现性调整**：仅限修复事实错误或程序错误、解决数值不稳定、补齐未明确的工程细节；调整不得改变检验假设、泄漏边界或外层数据用途。调整前须在 `version.md` 登记证据、影响范围、替代方案与回退条件。
4. **升级规则**：若必须改变冻结项，复制为新版本并显式递增版本号；保留本冻结版，不覆盖、不静默修改。测试集揭晓后不得反向选择分析窗、阈值、模型层级或删除阴性结果。
5. **实施顺序**：先完成事件解析与数据审计，再实现无泄漏划分和 B1/M0/Q3-M0 基线，随后逐级加入 B2–B4、M1、Q3-M1/M2；每一级只有通过本文准入条件才进入正文结论。
6. **冻结校验**：本文件的 SHA-256 校验值记录在根目录 `version.md`。任何改动都会改变校验值；编码启动前必须核对，若不一致则先停止并审查变更。
