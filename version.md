# 版本记录

## 2026-09-23：C 题完整方案 v4 冻结

- **状态**：已冻结，可作为后续编码与调试的唯一设计基线。
- **基线文件**：`C题高分完整方案_v4.md`
- **SHA-256**：`4F8D13087283FA6E52EA960B518473448E8A570264872CACFD94B0F380F37FC6`
- **历史文件**：`C题分析.md`、`C题高分完整方案.md`、v2、v3 仅供追溯，不再作为实现依据。

### 本次冻结修订

1. 将可靠度加权观测从模式空间改为 Fz/F3/F4 传感器空间：`w=0` 时删除对应观测行，全部传感器不可用时只做状态预测；正可靠度数值下限不得替代缺失观测。
2. 修正 xDAWN 与 ASR 的学术边界：3 通道 xDAWN 并非必然退化为标量；8 通道低密度 ASR 已有有效性证据，但不能直接外推至 3 通道长平台削顶。
3. 将问题一主分析明确为“题面驱动、编码前冻结”，禁止表述成独立预注册。
4. 将竞争累积模型限制为正确反应时分布的预测候选；100% 正确、无漏答的数据不足以独立辨识阈值、扩散噪声和起点偏置，恢复失败时降级到简单分布模型。
5. 将“可靠度加权必须优于硬剔除”改为可证伪的外层比较，不预设 B4 胜出。
6. 补充低密度 ASR、多层状态空间 ERP 与预处理—解码权衡的直接文献，并保留逐条核验要求。
7. 增加冻结执行规则：冻结项、允许的实现性调整、升级条件、实施顺序与校验流程。

### 版本级经验

- 传感器缺失不能先投影成模式级平均可靠度，否则一个通道的饱和可能被其他通道掩盖；缺失处理应在观测空间完成。
- “低通道效果可能有限”不等于“算法数学上退化或不适用”；竞赛论文必须把工程取舍与文献事实分开。
- 分析计划在看过数据后冻结，可以称题面驱动的确认性分析，但不能包装成预注册。
- 机制参数的可解释性取决于可辨识性和恢复实验，而不是模型名称复杂或拟合曲线好看。
- 外层测试集只负责一次最终评估；若结果不理想，应执行预设降级规则，不能反向修改分析窗或阈值。

### 后续变更流程

1. 编码启动前重新计算基线文件 SHA-256，并与本记录核对。
2. 若仅修复实现错误或数值问题，先在本文件新增记录，再实施并给出回退条件。
3. 若改变事件定义、数据划分、主分析、模型层级、准入/降级规则或学术主张，必须复制为新版本，不得覆盖 v4 冻结版。
4. 测试集揭晓后禁止结果驱动的方案修改；阴性结果必须保留。

## 2026-09-23：问题一实现记录 1

- **事项**：补齐硬剔除基线 B2 的峰峰值工程阈值为 400 微伏；饱和判据仍为绝对值等于 1000。
- **证据**：方案已冻结的四组提示窗存活试次数为 67、43、51、40；对原始数据逐文件只读核验后，400 微伏与四组记录同时一致。
- **影响范围**：仅用于描述性质量审计和 B2 硬剔除候选，不用于 B4 可靠度软阈值，也不允许外层测试折参与选择。
- **替代方案**：内层训练折采用稳健分位数或中位绝对偏差估计峰峰值阈值。
- **回退条件**：若后续核实数据单位并非微伏，或题目原始说明给出不同固定阈值，则撤销 400 微伏口径并重跑问题一全部下游输出。
- **经验**：冻结方案引用了复现计数时，应同时冻结产生该计数的阈值；否则实现阶段只能通过审计重建，增加歧义。
