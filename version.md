# 版本记录

## 2026年9月24日 问题一可复现分析

- 编码前提交已有暂存状态，提交号为107e256；本次编码后不自动提交。
- 首先依据题目和提示与动作关系核对实验：实验项目按任务编号分组，不按文件字母分组；字母的受试者含义未获证实。
- 实测第九通道兼有正负一和正负二两类编码，不将所有非零采样点当作点击或独立事件。
- 仅使用前三个原始脑电通道，保留原始幅值单位，不凭常识假定为微伏。
- 对饱和、基线异常和高频伪影做与标签无关的筛除及降权；对弱响应保留不确定性，不强行拟合成可靠正峰。
- 验证采用跨记录留出及带间隔的连续分块，标准化在训练折内完成；额外报告刺激前负对照和分块标签置换。
- 全部数据计算在服务器进行，显卡用于重采样和分类线性代数；滤波和小样本曲线拟合使用服务器处理器。
- 服务器远程工具的环境激活对复合命令不可靠，正式运行明确指定专用环境解释器绝对路径。
- 只有额区三通道、缺少眼电和受试者身份，不能据此验证中央顶区拓扑，也不能宣称完全去除眼动或证实纯形状编码。

- 实际结果显示项目二窗口最大值全部触及五百毫秒边界，增加局部正峰与宽窗描述性拟合，避免将慢波误标为目标成分。
- 保留受限拟合的失败及跨记录负决定系数；样本内高拟合优度不用于掩盖泛化不足。
- 最终十一幅图全部通过对齐与文字碰撞审计，七项科学计算不变量测试通过；共保留三百零八个试次，左右跨记录分类接近随机。

## 2026年9月24日 问题二最小可行版本

- 编码前提交问题一现有代码与片段，提交号为61eda6e；问题二完成后不自动提交。
- 复用问题一清洗片段，核对四个原始文件与缓存散列、通道顺序、刺激起点和原始基线片段；四百事件中描述性结果保留三百零八试次。
- 原始提示与动作通道标签包含方向编码后缀，不能按题面简写字符串完全匹配；实际兼容后仍严格检查前三脑电通道和二百五十六赫兹采样率。
- 全记录零相位滤波及全记录小波阈值不宜直接用于组内分块验证。严格验证沿用相同处理方法，但改为独立二十试次块滤波和单片段小波阈值。
- 描述性三百零八片段与严格分类三百一十九个硬质控合格片段属于不同用途；训练软异常阈值不得用于挑选测试样本，必须报告覆盖率和原因。
- 十五参数三源模型仅在训练记录拟合，简单与公共源上升扩展仅内部验证比较；保留三初值、不做大规模搜索。任务二训练曲线能表示晚期上升，任务一与跨记录拟合偏弱，多初值和边界参数不稳定。
- 严格左右对称混合使中额左右预测相同，任务一训练曲线存在结构性拟合上限；不能靠无限调参或测试集调参弥补。
- 正式主入口完整重跑后，真实跨记录主结果准确率50.16%、平衡准确率50.23%，两外层曲线下面积均值0.4921；均接近随机，不为得到较高测试分数改选方案。
- 先给定刺激标签生成左右源，再据此宣称源分类完美属于标签泄漏。真实源特征必须由未知方向脑电反演；无噪声模板间距只作假说示意，衰减不等于准确率自动下降。
- 通过既有远程工具上传、计算和下载；使用专用环境，按空闲显存选择编号一显卡处理平均和投影，处理器完成小样本拟合与传统分类。
- 三十六项数值及无泄漏检查通过，七组图形全部通过对齐与碰撞核验；统一入口的上传执行下载也经实际验证。
- 正式保留可复现片段、模型、特征和折外预测；试运行副本与失败运行目录不作为交付，不覆盖问题一原成果。任务一点击缺失、任务二正确性编码不确定均如实保留。
- 临时副本清理命令被安全限制拦截，尚未删除；已将其排除于问题二版本收录范围，正式交付只认定输出下的最终目录。未改用另一种删除方式绕过限制。

## 2026年9月24日 问题二源空间分类迭代

- 按用户说明从迁移后的问题二目录复用代码。编码前提交现有成果，提交号e9830b8；本轮完成后未提交，也未修改正在独立变动的问题三文件。
- 新建精简迭代目录，仅四个源码文件；原始前三通道输入，保留三百一十九个预先固定硬质控合格试次，不按测试预测或软异常删样本。
- 采用训练刺激前噪声协方差白化的岭反演，得到三源时间轨迹；训练内部选择反演强度、特征数量和线性分类器参数。
- 传统十五维、源十二维、联合十五维；联合固定十个源特征及五个头皮辅助特征，标准化后头皮特征乘零点五。一范数逻辑回归仅用于训练内排序，最终分类器按要求为二范数逻辑回归、线性支持向量机或收缩线性判别。
- 双向跨记录及每记录组五折时间块外层全部运行；每个内层重新拟合三源模型与所有统计，不复用含当前验证数据的旧模型。相同训练索引才允许共享本轮拟合，共三十二个不同模型训练集合。
- 块间隔必须按原始完整二十试次块边界定义，不能因边界试次先被硬质控剔除而缩短；发现后修正并完整重跑，未改候选或利用测试成绩追加调参。
- 最终源方案跨记录准确率50.47%、平衡准确率50.57%、曲线下面积0.4911；组内准确率52.98%、平衡准确率52.99%、曲线下面积0.5367。没有达到百分之六十五目标。
- 联合组内准确率55.80%；按内层选定主方案跨记录准确率49.84%、组内55.49%。不能用单个超过百分之七十二的时间块冒充总体效果，也不能依据测试表改选方案。
- 源与头皮在协方差度量下的马氏距离一致。反演是确定变换，不创造额外标签信息；高斯插件参照不是可靠分类上限，也不是外层准确率。
- 记录间刺激前噪声与平均曲线确有差异，但没有阻抗测量或明确被试身份，不能确定非神经因素是因果来源。反对称增益约0.0232与条件数约43.09仅是原拟合诊断，不证明分类随机的物理必然性。
- 本数据已在之前问题二被观察，必须披露本轮是探索性迭代，不伪称新的确认性外部测试。
- 通过远程工具在专用环境完成计算，最空闲显卡处理反演与频谱，处理器完成生成模型拟合及传统分类。最终三百二十五项核验和三组图形审计通过，源码散列一致；本轮试运行副本已安全清理，只保留正式复现产物。

## 2026年9月24日 问题二迭代目录合并

- 将分类迭代源码和正式结果合并到 Ques2；原问题二的 model.py、utils.py、入口和结果保持不变。
- 为避免同名辅助模块覆盖，迭代辅助文件改为 iter_utils.py；迭代结果改存 Ques2/outputs/iter_final/，默认运行输出使用独立时间戳目录。
- 更新迭代入口的导入、远程上传目录和服务器命令；保留原始历史清单中的路径与源码散列，不改写历史执行来源记录。
- 已核对一百零六个原问题二结果文件和一百八十四个迭代结果文件的散列；本地八个 Python 文件语法检查、两个入口帮助检查及服务器导入检查通过。
- 服务器使用 cuda:1 复算三十六个已保存模型包，共一千九百一十四条折外预测；保存特征和预测均一致，未重新训练。迁移检查记录在 Ques2/merge_check.json。

## 2026年9月24日 问题三最小认知模型

- 编码前提交号为488e576；复用问题一与原问题二正式结果，不覆盖已有计算成果，也不使用后续分类迭代测试成绩挑选问题三方案。本轮结束不提交。
- 四百次提示与缓存、原始事件逐条对应；任务一二百次无点击记录，只作视觉对照。任务二二百次点击中，按用户指定的方向一致性定义为九十五次正确、一百零五次错误；该定义不替代形状与位置匹配任务的真实答案键。
- 真实点击约在提示后三点五秒，不能把无应答二秒假设用于删除晚点击。无明确超时记录时，二秒只应用于缺失应答窗口；真实点击前至少一百毫秒结束脑电评价窗。
- 每折复用仅由训练记录拟合的问题二源模型，重新校准训练源差尺度，不输入测试提示、点击方向或点击时间。早期整窗证据约零点八秒后才读入模型，晚期源为动力学外推，不冒充实测源定位。
- 采用六参数记忆决策模型，固定记忆尺度与零先验，消除决策共同尺度；每折一百六十个候选加一次局部精调。离散阈值目标不光滑，局部优化终点可能差于已访问点，必须保留实际访问的最低训练损失，不按测试成绩选参数。
- 双向留组覆盖全部二百个任务二试次：二分类准确率53.00%、平衡准确率50.00%，左召回率为零、右召回率为一。模型退化为右偏，不能把多数类准确率描述成有效认知解码。
- 真实阈值准确率52.50%，一百九十八次产生越阈时刻；平均绝对误差0.2586秒、均方根误差0.3382秒、秩相关-0.1307，弱于训练中位数0.1314秒的平均绝对误差。不补填另两次越阈时间，不训练没有真实样本的遗漏类别。
- 脑电重建硬质控覆盖一百五十二次测试，六项通道比较均改善，平均决定系数由0.2242提高到0.3227。结果是离线重建，零相位滤波可能传播点击附近信号，不能宣称在线因果预测、海马测量或机制证实。
- 指定环境中由空闲显存较多的编号一显卡完成反演、批量候选递推与岭回归；处理器执行小规模局部优化。五十三项重放及隔离检查、五组图形碰撞检查通过。
- 成功续跑必须清除历史暂停标记，重新运行期间或失败后不得沿用旧完成状态。仅保留正式复现状态、参数搜索表和图形源数据，清理本轮已失效诊断文件。
