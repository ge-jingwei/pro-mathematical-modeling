# 版本记录

## 2026-09-23：P0 数据侦察与 EDA v1

- 完成四个 MAT 文件的结构、时间戳、事件、试次、RT、原始信号质量和 PSD 审计。
- 经验一：不能把题面给出的第 9 通道编码机械套用到全部文件。项目 1 实际是持续 ±1 的 Action，项目 2 才以单点 ±2 表示点击，同时包含持续 ±1 状态。
- 经验二：项目 1 RT 高度量化，MAD 可为 0；稳健离群尺度必须至少为一个采样周期，否则会把一采样点差异误报为异常。
- 经验三：多个原始通道存在 ±1000 饱和平台，后续必须先标记饱和区段，再进行保留 ERP 形态的去漂移与伪影拒绝。
- 经验四：50 Hz 未明显抬升，但多数通道在 60 Hz 出现窄带峰；后续滤波应先验证 60 Hz 陷波对 ERP 波形和相位的影响。
- 经验五：透明图例可能遮挡数据线并触发可读性问题。改用标题中的直接编码说明后，全部 PDF 碰撞审计通过。
- 交付：P0_方案.txt、P0_结果.csv、P0_图表、tools/p0_eda.py。

## 2026-09-23：P1 P300 提取 v1

- 完成四文件原始 Fz/F3/F4 的零相位滤波、小波降噪、分段、基线、伪影拒绝、ERP、Gamma 参数化、左右检验和图表审计；未使用 Decon 通道。
- 经验一：sym9 连续小波的 d5 约覆盖 4—8 Hz，对其施加通用阈值会削弱一致性 ERP；将 d5 的 α 设为 0、在更高频层递增阈值后，四文件 SNR 增益全部转正，Gabor 形状角仍远低于 30°。
- 经验二：单 Gamma 若允许峰落到窗外，会把 600 ms 后慢上升误作 P300。用峰值参数直接约束 250—500 ms，并加入二次慢背景项，可分离目标瞬态和背景趋势；目标参数仍只由 Gamma 分量定义。
- 经验三：固定 ±100 μV 在任务 2 会造成过度剔除。保留饱和硬剔除，同时在全文件统一候选阈值中选取首个使保留率严格大于 70% 的最小值，比按条件调阈值更透明。
- 经验四：左右靶的稳健差异出现在潜伏期而非振幅；合并文件×通道潜伏期检验 p=0.014、Holm p=0.029。应报告振幅未显著，不能通过事后筛选追求显著。
- 经验五：固定多面板轴位置可避免数据范围改变时 constrained layout 引起的间距审计失败。最终 15 个适用面板对齐审计、16 个字体审计和 16 个碰撞审计全部通过。
- 交付：P1_方案.txt、P1_结果.csv、P1_图表、tools/p1_p300.py。

## 2026-09-23：P2 EEG 级联计算模型推导 v1

- 完成 LGN DoG、V1 六方向双相 Gabor、COSFIRE 空间组合、双节点 Jansen—Rit、三层球体积传导、3×2 Lead Field、八维判别特征与 LDA/BLDA 的完整推导；本阶段未编写代码。
- 经验一：左右三角的有向边集合在模 180° 后相同，单纯全局池化的无向 Gabor 能量不能区分镜像；必须保留空间排列、相位/极性或顶点相对位置，并用镜像 COSFIRE 配置形成差异向量。
- 经验二：对侧视觉加工严格依赖刺激所在视觉半场，不能把“中央三角的指向”直接等同于视觉半场；形状方向和目标空间侧需要分开建模。
- 经验三：标准 JR 的 10—20 ms 局部突触时间尺度不能单独解释约 300 ms 的 P300；需要可解释的刺激包络、区域级联和传输/整合时延。
- 经验四：仅有 Fz/F3/F4 时，参考后的观测有效秩至多为 2；两个等效源必须固定几何先验，不能宣称完成唯一脑源定位。
- 经验五：联网核验发现 Glomb 等 2022 综述的正确 DOI 为 10.1007/s10548-021-00828-2。
- 交付：P2_方案.txt。

## 2026-09-24：P3 EEG 计算模型实现与验证 v1

- 完成规范三角 DoG/Gabor 空间编码、双节点 Jansen—Rit、3×2 等效 Lead Field、模拟—实测比较、BLDA 分类和 C/w 灵敏度分析；真实数据仅使用 Fz/F3/F4。
- 经验一：镜像三角的六方向无向 Gabor 全局能量差严格退化为 0；加入左右半区空间池化后 L2 差异为 0.340516，形状区分必须保留空间布局。
- 经验二：222 个合格正确试次上的单试次基础 BLDA 仅为 53.153%；10 提示块重复试次集成达到 63.158% 的兜底标准，20 提示块因有效样本降至 26 个反而退化，不能用更多平均次数掩盖样本量损失。
- 经验三：模拟在全部 6 个条件×通道中产生 250—500 ms 正峰，标准 C 下 V1/PFC 主频为 8/11 Hz；但波形中位相关仅 0.015、潜伏期误差 114.6 ms，且 F3−F4 趋势不一致，只能解释时窗和频段机制，不能冒充精确拟合。
- 经验四：固定时间块分组、训练折内标准化与模板构建能避免相邻试次和模板泄漏；随机五折并未显著优于分组五折，低性能主要来自三电极单试次信噪比不足。
- 经验五：六幅核心图均以 600 dpi PNG 和 PDF 输出，对齐、最小字体与碰撞审计全部通过。
- 交付：P3_方案.txt、P3_结果.csv、P3_图表、tools/p3_model.py。

## 2026-09-24：P3 单试次分类优化 v2

- 在严格分组五折、训练折内标准化/填补/降维/模板学习的条件下完成 26 组特征与模型比较；原 541 行基线结果完整保留，优化记录以新行追加。
- 经验一：当前最优为全特征正则协方差模型，222 个折外试次准确率与平衡准确率均为 56.306%，高于 53.153% 基线但未达到 60% 兜底，必须如实报告三电极单试次信息不足。
- 经验二：单试次 Gamma 拟合成功率 99.40%，但 R² 中位数仅 0.664；形态参数、扩展频相特征、PCA、任务/被试分层与 CSP 均未带来可复现增益，说明“可拟合”不等于“可判别”。
- 经验三：左右镜像增强降至 46.847%，表明 F3/F4 的真实个体与任务不对称不能用理想半球镜像替代；该增强不应进入最终模型。
- 经验四：所有尝试 1.47 分钟完成，未触发 90 分钟止损；因后续回退方法均低于当前最优，停止进一步搜索以限制同一数据上的模型选择偏倚。
- 交付：P3优化_方案.txt、追加后的 P3_结果.csv、P3优化_对比图.png/PDF、tools/p3_optimize.py。
